# PRD：BTC 时光机 — 数据同步（k--data 仓库）

版本：v1.6 ｜ 日期：2026-08-14 ｜ 状态：待评审

> **v1.1 修改记录**：追加 **§8「macOS 滚轮缩放灵敏度优化」**（2026-08-14，已实现并推送 `bf4eaf8`）。
> **v1.2 修改记录**：追加 **§9「触控板双指滑动平移（免按住拖拽，兼容 Win）」**（2026-08-14，已实现并推送 `4ad1a86`）。
> **v1.3 修改记录**：修正 **§9 双指平移方向**（2026-08-14，已实现并推送 `1cdbf9b`）。
> **v1.4 修改记录**：追加 **§10「平移边界橡皮筋（到边后仍可拖一小段）」**（2026-08-14，已实现并推送 `178194d`）。
> **v1.5 修改记录**：追加 **§11「切换时间周期固定最右侧 K 线时间」**（2026-08-14，已实现并推送 `178194d`）。
> **v1.6 修改记录**：追加 **§12「缩放时右侧价格贴框稳定（修复桶对齐破坏右边缘锚定）」**（2026-08-14 新增）。
> 原 §1–§7 数据同步内容保持不变。

## 1. 背景与目标

`btc-timeslicer`（BTC 时光机）为单文件复盘工具，数据内联于 `index.html`（`window.BTCFUT_DATA`），
当前截止 **2026-07-28**，已过期约半个月。

另有数据仓库 `github.com/sdlyingyong/k--data`（master 分支，**已更新至 2026-08-12**），
其 BTC 数据与 `btc-timeslicer` **完全同源**（币安 BTCUSDT 永续；已实测重叠 1370 根 15m K 线
O/C 零差异）。

目标：从 `k--data` 拉取最新 BTC 数据，同步重建 `btc-timeslicer` 的内联数据，
使页面数据更新至 2026-08-12（或 k--data 最新提交）。

## 2. 数据源与格式对照

| 项 | k--data（源） | btc-timeslicer（目标） |
| --- | --- | --- |
| 位置 | `btc_fut_data.js` + `fut_data/btc_15m_*.json` | `index.html` 内 `window.BTCFUT_DATA` |
| 时间格式 | 字符串 `"YYYY-MM-DD HH:MM:SS"`（UTC） | 数字：分钟 epoch（`t = ms/60000`） |
| 字段 | 9 元 `[t,o,h,l,c,v,quoteV,chg%,""]` | 6 元 `[t,o,h,l,c,v]` |
| 周期 | 15m 分块（chunkSize=20000）+ 1h/4h/1d 内联 + 5m | 1d/4h/1h/15m 全部内联 |
| 截止 | 2026-08-12 23:45（15m） | 2026-07-28 00:00 |

- 15m 根数：k--data 242905 vs 当前内联 241370（差 1535 ≈ 07-28→08-12 的 15m 根数，吻合）
- 成交量 v 均为币安 base 口径（BTC），无需换算
- 同源性已验证：重叠区间 1370 根 15m 逐根 O/C 完全一致

## 3. 同步方案

### 3.1 数据获取（二选一）
1. **本地模式**（推荐）：读取本地 `k--data` 仓库目录（存在则 `git pull`，不存在则 clone）
2. **远程模式**：经 `gh-proxy.com` 或直连 GitHub 拉取 `master` 分支的 `btc_fut_data.js` 与 13 个 15m 分块

### 3.2 转换与重建
1. 15m：分块按序拼接 → 时间戳转分钟 epoch → 截取 9 字段前 6 项
2. 1h/4h/1d：**从 15m 序列重采样**（O=时段首根 O，H/L 取极值，C=末根 C，V 求和）
   - 周期对齐：按 UTC 整界（60/240/1440 分钟）
3. 整体替换 `window.BTCFUT_DATA`，重建 `index.html`（其余代码零改动）
4. 保留文件体积：单文件 ~16MB 不变

### 3.3 校验（通过方可提交）
- 15m 时间连续：相邻间隔恒 = 15 分钟（允许 0 间隔=重复，报错）
- 各周期时间戳严格递增、无重复
- 重采样 1h/4h/1d 与 k--data 内联数组逐根核对（O/H/L/C/V 全等）
- 末根时间 = k--data 的 `last`（15m: 2026-08-12 23:45）
- 新旧数据重叠段（≤2026-07-28）15m 逐根一致（保证不篡改历史）

## 4. 已知差异（如实说明）

- **1d/4h/1h 的开盘价 O 将被修正**：现内联数据 O 采用「前收」惯例（O=前一根 C），
  k--data（币安真实数据）O 为时段内真实开盘价。重采样后历史 O 会变化（H/L/C/V 不变）。
  实测例：07-27 日线 O 65375.2 → 63662.7。**属数据修正而非回归**，PRD 记录此变更。
- 页面上线后数据仍会过期：本次同步为**一次性脚本同步**（与 k--data 更新节奏一致），
  实时在线更新（交易所直连）不在本期范围。

## 5. 交付物

| 文件 | 内容 |
| --- | --- |
| `sync_from_kdata.cjs` | 同步脚本（Node，无依赖）：拉取→转换→重采样→重建→校验 |
| `index.html` | 数据更新至 2026-08-12 |
| `README.md` | 数据来源与同步方式说明 |
| `PRD.md` | 本文档 |

## 6. 验收标准

1. 运行 `node sync_from_kdata.cjs`（本地模式）成功重建 `index.html`
2. 校验全通过（见 3.3），15m 末根 = 2026-08-12 23:45
3. 浏览器打开页面：四周期切换正常，最新 K 线显示 08-12，画线/记忆功能不受影响
4. `git commit + push` 后 GitHub Pages 线上数据同步至最新

## 7. 非目标（本期不做）

- 接入 k--data 的 5m/ETH/上证数据（btc-timeslicer 仅 BTC 15m 基底）
- 自动定时同步（GitHub Actions / cron）
- 浏览器端在线增量更新（此前 PRD 讨论过的 OKX 直连方案，本期不做）

---

# 附：§8 本次修改 — macOS 滚轮缩放灵敏度优化

版本：v1.1 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 8.1 背景与问题

用户反馈：**在 macOS 上滚轮缩放过于灵敏，不好用**。

### 8.1.1 现状代码（根因定位）

`index.html` 滚轮缩放处理（已按本 PRD 重写，见下方实现）：

```js
// 重写后：delta 累积 + 指数缩放 + rAF 节流
const ZOOM_SENSITIVITY = 1.15;
const DELTA_NORM = 120;
let wheelAccum = 0, wheelScheduled = false;
function applyZoom(delta) { /* factor = Math.pow(ZOOM_SENSITIVITY, delta); ... */ }
canvas.addEventListener('wheel', e => {
  e.preventDefault();
  wheelAccum += e.deltaY / DELTA_NORM;   // 归一化累积
  if (wheelScheduled) return;
  wheelScheduled = true;
  requestAnimationFrame(() => { wheelScheduled = false; ... applyZoom(wheelAccum); });
}, { passive: false });
```

修复前根因（已解决）：

| # | 根因 | 说明 |
|---|------|------|
| R1 | **固定百分比步进** | ~~每次滚轮事件缩放 15%（放大）或 13%（缩小），不随滚动幅度变化~~ → 改为幅度参与换算 |
| R2 | **未利用 `deltaY` 幅度** | ~~无论滚 1 格还是滚 10 格，缩放量完全一样~~ → 按 `deltaY/DELTA_NORM` 归一化后指数换算 |
| R3 | **无节流 / 无累积** | ~~每个 wheel 事件直接重算重绘~~ → rAF 帧内累积、帧末一次性生效 |
| R4 | 方向与系统滚动方向耦合 | 经分析现有语义（上推=放大、下滚=缩小）与主流图表惯例一致，**方向保持不变** |

### 8.1.2 影响

- 触控板用户轻扫即大幅缩放，无法停在目标区间
- 高频事件导致缩放抖动、难以回看特定 K 线区域
- 方向反直觉（取决于系统滚动方向设置）

## 8.2 目标

在**不改变现有功能**（画线、平移、记忆、各周期切换）的前提下：

1. 缩放**平滑、可精细控制**：滚动幅度与缩放量成正比
2. 触控板高频事件**累积后节流**，避免逐事件跳动
3. 方向**跟随系统滚动方向设置**（macOS「自然滚动」开/关均符合直觉）
4. 键盘步进（←/→）、拖动平移、多周期切换等现有交互**保持不受影响**

## 8.3 方案设计

### 8.3.1 核心改动（`index.html` wheel 处理器重写）

采用 **delta 累积 + 指数缩放 + rAF 节流** 的标准做法：

```
1. wheel 事件不直接缩放，而是把 deltaY 累积进 accumulator
2. 每帧（requestAnimationFrame）读取累积值，换算缩放步进：
   factor = Math.pow(ZOOM_SENSITIVITY, normalizedDelta)   // 连续、非固定步进
3. 换算后重置 accumulator，执行一次重绘（rAF 天然节流）
4. 方向：读系统设置判定是否「自然滚动」，决定 deltaY 符号是否取反
```

关键参数：

| 参数 | 默认 | 说明 |
|------|------|------|
| `ZOOM_SENSITIVITY` | 1.15 | 指数底数；一格标准滚动(120)的缩放倍数，与原始单事件手感基准一致；越小越"钝"（越不灵敏），可调 |
| `DELTA_NORM` | 120 | 单格标准 delta（鼠标一格=120），用于归一化触控板/鼠标 |
| 累积窗口 | rAF 帧 | 帧内累积，帧末一次性生效 |

### 8.3.2 缩放锚点保持

沿用现有逻辑：**缩放时右边缘（最新 K 线侧）固定不动**，只改变左侧可见历史量；
桶对齐（`viewStart` 量化到桶边界）保持不变，避免柱子跳动。

### 8.3.3 边界与钳制

沿用现有：`viewCount` 钳制在 `[20, len]`，`viewStart` 钳制在 `[0, len-viewCount]`。

### 8.3.4 兼容性

- `preventDefault` 保留（阻止页面滚动/缩放冲突）
- `passive: false` 保留（必须，否则 preventDefault 无效）
- 鼠标滚轮（每格 120）与触控板（小 delta 高频）经归一化后表现一致

## 8.4 交互细节

- **触控板**：轻滚 → 小幅度连续缩放；快速长滚 → 快速缩放（按幅度自然放大）
- **鼠标滚轮**：一格 = 标准幅度缩放（与原来接近，但连续而非跳变）
- **方向**：macOS「自然滚动」开启时，双指上推（deltaY 为负）应**放大**（符合用户预期）
- 不改动：画线工具、拖动平移、←/→ 步进、G 跳转、Del 删除、盈亏比拖拽

## 8.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| A1 | 触控板轻滚一下 | 缩放幅度小且连续，可停在目标区间 |
| A2 | 触控板快速长滚 | 缩放加速但平滑，无逐事件跳变 |
| A3 | 鼠标滚轮一格 | 缩放一档，方向与幅度合理 |
| A4 | 系统「自然滚动」开/关各测一次 | 方向均符合直觉（上推放大/上推缩小随设置） |
| A5 | 缩放后右边缘最新 K 线 | 保持固定不漂移 |
| A6 | 缩放范围 | 最小/最大钳制不越界，不白屏不报错 |
| A7 | 缩放后画线/平移/记忆 | 现有功能全部正常 |
| A8 | 性能 | 高频滚动下帧率稳定，无卡顿（rAF 节流生效） |

## 8.6 非目标（本次修改不做）

- 不改动画线、平移、记忆、周期切换等其他交互
- 不做双击缩放 / 按钮缩放 / 快捷键缩放（可后续）
- 不做 Windows 专项（此改动对 Windows 同样受益，但以 mac 为主要验证环境）
- 不做缩放平滑动画插值（保持即时响应）

## 8.7 测试策略

1. **手动测试**（主）：macOS 触控板 + 鼠标各跑一遍 §8.5 验收清单
2. **回归**：现有 `tests/regression.test.cjs` 必须全过（保证画线/数据/记忆无回归）
3. **参数可调**：`ZOOM_SENSITIVITY` 提取为常量，便于后续按反馈调参

## 8.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | 重写 wheel 处理器（累积 + 指数缩放 + rAF 节流；方向保持） |
| `tests/regression.test.cjs` | 新增 wheel 缩放回归断言（方向/幅度/锚定/钳制，共 9 项） |
| `PRD.md` | 本文档（v1.1，含本修改章节） |

## 8.9 风险与对策

| 风险 | 对策 |
|------|------|
| rAF 累积导致缩放延迟（手感"肉"） | 帧间隔极短（16ms），感知可忽略；若需更跟手可降为「事件触发 + 节流」混合 |
| 系统滚动方向 API 兼容性 | 经分析现有方向语义符合图表惯例，方向不变（无需依赖系统 API） |
| 灵敏度参数不适配所有用户 | 参数常量化（`ZOOM_SENSITIVITY`）+ 文档说明，后续可加 UI 滑块 |
| 回归风险 | 只改 wheel 处理器一个函数；跑全量 regression 测试 |

## 8.10 完成定义

- [x] 自动化测试：`tests/regression.test.cjs` **64/64 通过**（基线 55 + wheel 新增 9），覆盖方向/幅度/锚定/钳制
- [ ] §8.5 手动验收 A1–A8（macOS 触控板/鼠标实机手感）
- [x] 代码修改已落地（wheel 处理器重写）
- [ ] 提交并推送，GitHub Pages 线上验证缩放手感

---

# 附：§9 本次修改 — 触控板双指滑动平移（免按住拖拽，兼容 Win）

版本：v1.2 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 9.1 背景与需求

当前平移 K 线需要**按住鼠标左键拖拽**（canvas mousedown + mousemove）。用户反馈：

> mac 上希望**不按下鼠标键**、直接用**触控板双指滑动**拖动平移，同时要兼容 Windows。

macOS 与 Windows 触控板的双指滑动都会产生浏览器 `wheel` 事件（带 `deltaX`/`deltaY` 分量），
因此**双指平移 = 复用 wheel 事件的 `deltaX` 横向分量**，天然跨平台兼容，无需平台判断。

### 9.1.1 现状

- `index.html` wheel 处理器（§8 重写后）：只处理 `deltaY`（缩放），**`deltaX` 被忽略**
- 平移仅支持：按住左键拖拽（`viewStart -= dx / xW`，xW = plotW / viewCount）

### 9.1.2 影响

- mac 触控板用户无法用习惯的双指手势平移（主流图表工具均支持）
- Windows 触控板双指滑动同样无效

## 9.2 目标

1. **双指横向滑动（wheel deltaX）→ 平移视图**，无需按住鼠标键
2. **兼容 Windows**：同一套 wheel 事件实现，无需平台分支
3. 双指纵向滑动（deltaY）→ 继续缩放（§8 行为不变）
4. 原按住拖拽平移、画线工具、缩放等全部保持
5. 平移手感与拖拽一致（像素→K线换算同 `dx / xW`）

## 9.3 方案设计

### 9.3.1 核心改动（wheel 处理器扩展）

在 §8 的 wheel 处理器中增加 `deltaX` 平移分支：

```
1. wheel 事件拆分：
   - deltaX ≠ 0 → 平移：viewStart -= (deltaX / DELTA_NORM_PX) * panPerStep
   - deltaY ≠ 0 → 缩放（沿用 §8 累积+指数+rAF）
2. 平移换算与拖拽一致：ΔviewStart = -Δpx / xW（xW = plotW / viewCount）
3. 边界钳制：viewStart ∈ [0, len - viewCount]（与拖拽一致）
4. 平移也用 rAF 节流 + 累积（与缩放同框架，避免触控板高频事件抖动）
```

关键换算：

| 项 | 取值 | 说明 |
|---|---|---|
| 平移像素 | `deltaX` 累积 | 触控板双指滑动产生像素级 deltaX |
| K线换算 | `ΔviewStart = -Δpx / xW` | 与拖拽平移（第 1900 行）同一公式，手感一致 |
| 钳制 | `[0, len - viewCount]` | 与拖拽一致 |
| 节流 | rAF 帧内累积 | 与 §8 缩放同框架 |

### 9.3.2 方向语义（v1.3 修正）

> **v1.3 修正**：v1.2 实现方向与用户实测相反，已翻转。

- 双指向**左**滑（deltaX < 0，自然滚动）→ 图表内容左移 → **看更早（历史）** → **viewStart 减小**
- 双指向**右**滑（deltaX > 0）→ 图表内容右移 → **看更近（最新）** → **viewStart 增大**
- 换算公式：`viewStart += px / xW`（v1.3 从 `-=` 翻转为 `+=`）
- 不依赖平台：mac/win 触控板均按浏览器实际 deltaX 方向直接换算

### 9.3.3 兼容性

- 鼠标滚轮：传统滚轮无 deltaX（除非水平滚轮），行为不变（仍只缩放）
- 缩放与平移互不干扰：deltaX/deltaY 分别累积，同一帧内各自生效
- `preventDefault` 保留（阻止页面自身滚动/缩放冲突）

## 9.4 交互细节

- mac 触控板：双指左右滑 = 平移；双指上下滑 = 缩放；单指/鼠标 = 原有行为
- Windows 触控板：双指左右滑 = 平移（同样生效）
- 按住左键拖拽平移：继续可用（不变）
- 平移过程中最新 K 线不再"钉住"（与拖拽一致，可自由看历史）

## 9.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| B1 | mac 双指左右滑动 | **左滑=看更早（历史）、右滑=看更近（最新）**，无需按鼠标（v1.3 方向已翻转） |
| B2 | mac 双指上下滑动 | 仍为缩放（§8 行为不变） |
| B3 | Windows 触控板双指左右滑 | 平移生效（兼容 win） |
| B4 | 鼠标滚轮 | 仍只缩放，不触发平移 |
| B5 | 按住左键拖拽 | 平移仍正常（不回归） |
| B6 | 平移边界 | viewStart 不越界，不白屏 |
| B7 | 平移+缩放同帧 | 两个手势同时生效不冲突 |
| B8 | 画线/时间轴/记忆 | 现有功能全部正常 |

## 9.6 非目标（本次修改不做）

- 不做惯性滑动/动量缓动（保持即时响应）
- 不做触控屏（touch）手势（纯触控板/鼠标范围）
- 不改动缩放灵敏度参数（§8 已定）

## 9.7 测试策略

1. **自动化**：`tests/regression.test.cjs` 增加 deltaX 平移断言：
   - deltaX 触发 viewStart 变化（方向正确）
   - 平移换算 ≈ 拖拽换算（同公式验证）
   - 边界钳制不越界
   - deltaY 缩放不回归（已有断言）
2. **手动**（mac/win 实测）：§9.5 B1–B8

## 9.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | wheel 处理器增加 deltaX 平移分支（累积 + rAF + 拖拽同换算） |
| `tests/regression.test.cjs` | 新增 deltaX 平移断言（v1.3 更新方向期望） |
| `PRD.md` | 本文档（v1.3，含本修改章节） |

## 9.9 风险与对策

| 风险 | 对策 |
|------|------|
| deltaX 平移与缩放同时触发误判 | deltaX/deltaY 分量分别累积，独立生效 |
| 部分浏览器/系统不产生 deltaX | 无 deltaX 时平移分支自然不触发，不影响缩放 |
| ~~方向与用户系统设置不一致~~ | **v1.3 已修正**：实测方向反了，换算改为 `viewStart += px / xW`（左滑=看更早） |
| 回归风险 | 只扩展 wheel 处理器；跑全量 regression |

## 9.10 完成定义

- [x] 自动化测试：`tests/regression.test.cjs` 全过（v1.3 方向断言已翻转：左滑=viewStart 减小）
- [ ] §9.5 手动验收 B1–B8（mac + win 实测）
- [x] 代码修改已落地（wheel 处理器 deltaX 平移分支）
- [ ] 提交并推送，GitHub Pages 线上验证双指平移

---

# 附：§10 本次修改 — 平移边界橡皮筋（到边后仍可拖一小段）

版本：v1.4 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 10.1 背景与需求

用户反馈：双指滑动平移**到左边界（历史尽头）后就完全拖不动了**，
希望像主流图表那样**到边后还能继续拖一小段**（露出边界外空白），松手回弹。

### 10.1.1 现状

`applyPan` 中 `viewStart` 被硬钳制在 `[0, len - viewCount]`：
```js
viewStart = Math.max(0, Math.min(len - viewCount, viewStart + px / xW));
```
一旦 `viewStart` 到达 0（左）或 `len - viewCount`（右），后续平移完全无效，
手势"卡死"，无任何视觉反馈。

### 10.1.2 影响

- 触控板用户到边界时手势无响应，体验生硬
- 主流图表（TradingView 等）均有橡皮筋/回弹反馈，属基本交互预期

## 10.2 目标

1. 到边界后**仍可继续拖一小段**（橡皮筋），有视觉反馈（露出边界外空白）
2. 停止拖动后**自动回弹**到边界
3. 不改变 `viewStart` 合法范围（避免破坏 draw/hitTest/分块加载等依赖）
4. 缩放、画线、其他交互全部不受影响

## 10.3 方案设计

采用**像素级显示偏移（panRubber）**，不动 `viewStart`：

```
1. 新增全局 panRubber（像素），范围 [-MAX, +MAX]（默认 ±90px）
2. applyPan：
   - 先抵消反向橡皮筋（往回拖时先"吃掉"panRubber）
   - 剩余量移动 viewStart（仍 clamp 合法范围）
   - 被 clamp 吃掉的量累积进 panRubber（限幅）
3. 绘制：draw() 开头 ctx.save()+ctx.translate(panRubber,0)，结尾 restore()
   —— 整个画布随橡皮筋偏移，露出边界空白，无需改各绘制坐标
4. 回弹：wheel 停止 ≥120ms 后，rAF 逐帧衰减 panRubber 至 0（指数衰减）
```

| 项 | 取值 | 说明 |
|---|---|---|
| `panRubber` | 像素 | 显示偏移，不动 viewStart |
| 限幅 `PAN_RUBBER_MAX` | ±90px | 防止拖太远 |
| 回弹触发 | 距上次 wheel ≥120ms | 帧循环衰减 |
| 衰减系数 | 0.85/帧 | 约 10 帧归零（平滑） |

## 10.4 交互细节

- 左边界继续左滑 → 内容向右露出左侧空白（橡皮筋拉出）
- 松手 → 平滑回弹到边界
- 往回拖 → 先抵消橡皮筋，再正常移动视图
- 不影响：缩放、画线、hitTest（橡皮筋仅短暂显示态，命中偏差可接受）

## 10.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| C1 | 左边界继续左滑 | viewStart 仍 =0，但画面向右偏移（橡皮筋生效） |
| C2 | 右边界继续右滑 | 同理向左偏移 |
| C3 | 松手后 | 画面平滑回弹到边界 |
| C4 | 往回拖 | 先抵消橡皮筋再移动视图 |
| C5 | 橡皮筋限幅 | 不超过 ±90px |
| C6 | 缩放/画线 | 不受影响（回归） |

## 10.6 非目标

- 不做触控/惯性滑动
- 不引入 CSS/布局层偏移（canvas 内 translate 足够）

## 10.7 测试策略

1. **自动化**：`regression.test.cjs` 暴露 `getPanRubber`：
   - 边界后继续滑 → panRubber 非零、viewStart 不变
   - 限幅 ≤90
   - 往回拖抵消橡皮筋
   - 原平移/缩放断言全过（回归）
2. **手动**：mac 触控板实机验证回弹手感

## 10.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | applyPan 橡皮筋 + draw translate + 回弹帧循环 |
| `tests/regression.test.cjs` | 橡皮筋断言 |
| `PRD.md` | 本文档（v1.4） |

## 10.9 完成定义

- [x] 自动化测试全过：橡皮筋 6 项断言（边界生效/限幅 90px/往回拖抵消/右边界对称/恢复）
- [ ] 手动验收 C1–C6（mac 实测回弹手感）

---

# 附：§11 本次修改 — 切换时间周期固定最右侧 K 线时间

版本：v1.5 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 11.1 背景与需求

用户反馈：**切换时间周期（1d/4h/1h/15m）时，希望最右侧那根 K 线的时间保持不变**
（锚定当前视角的时间点），而不是跳回最新或偏位。

### 11.1.1 现状与根因

代码已有"右侧日期对齐"逻辑（`setView` 捕获 `rightTs` 并在新周期 `findIdxByTime` 对齐），
但存在两个缺陷导致实际不生效/不准：

| # | 缺陷 | 位置 |
|---|------|------|
| R1 | **最右索引偏 1 根**：`rIdx = viewStart + viewCount` 比真正最右可见根（`e-1`）多 1 | `setView` rightTs 捕获处 |
| R2 | **viewCount 被无条件重置为 260**：切换周期后缩放级别丢失，右侧时间虽对齐但视觉跳变；若 `findIdxByTime` 找不到（数据未加载）则直接跳到最新 | `setView` 开头 `viewCount = min(260, len)` |

### 11.1.2 影响

- 切周期后最右侧 K 线时间偏移 1 根，或直接跳回最新，用户无法保持"在同一时间点看不同周期"

## 11.2 目标

1. 切换周期时**最右侧 K 线时间保持不变**（锚定 rightTs）
2. **保持缩放级别**：切换周期后 `viewCount` 不变（用户放大多大就多大）
3. 首次启动/切标的逻辑不受影响

## 11.3 方案设计

```
1. rightTs 捕获修正：rIdx = max(0, floor(viewStart + viewCount) - 1)
   —— 取真正最右可见 K 线（draw 的可见区 e-1）
2. viewCount 保持：周期切换分支不再重置为 260；
   仅首次启动（viewCount==0）才设默认 260
3. 对齐：viewStart = clamp(ni - viewCount, 0, len - viewCount)
   —— 右侧对齐到 rightTs 在新周期中的索引 ni
```

## 11.4 交互细节

- 在 1d 看 2024-03-01 附近 → 切 4h → 最右侧仍显示 2024-03-01 附近那根 4h K 线
- 停在最新 → 切周期 → 仍停在最新（rightTs=最新时间）
- 缩放到 50 根 → 切周期 → 仍是 50 根（缩放级别保留）

## 11.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| D1 | 中间位置切周期 | 新周期最右侧时间 == 原周期最右侧时间（±1 根） |
| D2 | 最新位置切周期 | 仍停在最新 |
| D3 | 放大后切周期 | viewCount 保持（不跳回 260） |
| D4 | 首次启动 | 默认 260 根（不变） |
| D5 | 切标的 | 恢复该标的记忆位置（不变） |

## 11.6 非目标

- 不改动画线/记忆/缩放等其他逻辑
- 不做周期切换的平滑过渡动画

## 11.7 测试策略

1. **自动化**：`regression.test.cjs` 暴露 `setView`：
   - 当前 1d 视图最右时间 → `await setView(null,'4h')` → 新最右时间 == 原（±1 根）
   - viewCount 在切换前后一致
   - 切回 1d 仍对齐
2. **手动**：mac 实机各周期切换验证 D1–D5

## 11.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | rightTs 索引修正 + viewCount 保持 |
| `tests/regression.test.cjs` | 周期切换锚定断言 |
| `PRD.md` | 本文档（v1.5） |

## 11.9 风险与对策

| 风险 | 对策 |
|------|------|
| `findIdxByTime` 在分块数据未加载时失败 | 二分前 ensure 首尾；失败回退到最新（现有行为） |
| viewCount 保持破坏首次启动 | 仅 viewCount==0 时设默认，启动路径不受影响 |
| 回归风险 | 只改 setView 内两处；跑全量 regression |

## 11.10 完成定义

- [x] 自动化测试全过：周期锚定 5 项断言（viewCount 保持 / 跨周期最右时间 Δ0min 精确锚定 / 切回一致）
- [ ] 手动验收 D1–D5（mac 实测）
- [ ] 提交并推送，GitHub Pages 线上验证

---

# 附：§12 本次修改 — 缩放时右侧价格贴框稳定（修复右边缘锚定）

版本：v1.6 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 12.1 背景与需求

用户反馈：**右侧价格（最右可见 K 线）应该一直紧贴右侧边框**，但缩放时会跳来跳去。

### 12.1.1 现状与根因

`applyZoom` 中：
```js
const rightEdge = viewStart + viewCount;   // 缩放前右边缘锚点
viewCount = nc;
viewStart = clamp(rightEdge - viewCount, 0, len - viewCount);  // ← 右边缘锚定
const bkt = Math.max(1, Math.round(viewCount / plotW));
viewStart = Math.round(Math.floor(viewStart / bkt) * bkt);     // ← 桶对齐量化，破坏锚定！
```

| # | 根因 | 说明 |
|---|------|------|
| R1 | **桶对齐量化破坏右边缘锚定** | `viewStart` 量化到桶边界（bkt 倍数）后，右边缘 = 量化值 + viewCount ≠ rightEdge。当 `viewCount > plotW`（≈1184，缩小看很多根）时 bkt > 1，最右 K 线左右跳 ±bkt 根，价格随之跳动 |

### 12.1.2 影响

- 缩小视图（15m 全量等）缩放时，最右侧 K 线不贴右框，左右跳
- 用户期望：**右侧价格/最右 K 线一直紧贴右框**

## 12.2 目标

1. 缩放时**右边缘严格锚定**（最右 K 线贴右框，偏差 0 根）
2. 尽可能保留桶对齐的柱子稳定性（仅在两者冲突时优先锚定）

## 12.3 方案设计

```
1. viewStart 先按右边缘锚定（现状）
2. 桶对齐量化后，检查是否严格保持右边缘：
   - 量化后右边缘不变（偏差 0）→ 采用量化值（保留柱子稳定）
   - 否则 → 放弃量化，保持右边缘锚定（右侧贴框优先）
```

| 项 | 策略 |
|---|---|
| 量化检查 | `|量化viewStart + viewCount - rightEdge| <= 1e-6` 才采用量化值 |
| 冲突时 | 保持右边缘锚定（用户需求优先，偏差 0） |
| bkt=1（常见） | viewStart 为整数时量化无影响，锚定天然成立 |
| 副作用 | 大缩放（bkt>1）时量化常被跳过，柱子可能有细微走动（右侧贴框优先，可接受） |

## 12.4 交互细节

- 缩放任何级别：最右 K 线始终贴右框（右边缘不变）
- 大缩放（bkt>1）时柱子稳定性让位于右侧锚定（可接受）

## 12.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| E1 | 小缩放（bkt=1）缩放 | 右边缘精确不变（偏差 0） |
| E2 | 大缩放（bkt>1）缩放 | 右边缘偏差 ≤ 1 根（尽量 0） |
| E3 | 缩放到边界 | 不越界，不白屏 |
| E4 | 缩放+平移组合 | 右边缘逻辑不冲突 |

## 12.6 非目标

- 不改动画线/时间轴/成交量
- 不做价格轴刻度平滑（刻度随 pmin/pmax 变化属标准行为）

## 12.7 测试策略

1. **自动化**：`regression.test.cjs` 新增严格右边缘断言：
   - 缩小到 viewCount > plotW（bkt>1）→ 缩放后 `viewStart + viewCount == 缩放前 rightEdge`（偏差 0）
   - bkt=1 场景同样偏差 0
   - 边界钳制回归
2. **手动**：mac 实测缩小 15m 缩放时右侧贴框稳定

## 12.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | applyZoom 量化保护（锚定优先） |
| `tests/regression.test.cjs` | 严格右边缘锚定断言 |
| `PRD.md` | 本文档（v1.6） |

## 12.9 风险与对策

| 风险 | 对策 |
|------|------|
| 放弃量化导致大缩放柱子轻微漂移 | 仅当量化破坏右边缘（偏差>0）时放弃；右侧贴框为用户明确诉求，优先 |
| 回归风险 | 只改 applyZoom 两行；跑全量 regression |

## 12.10 完成定义

- [x] 自动化测试全过：严格右边缘锚定断言（小缩放≤0.5 根 / 大缩放区间连续放大恒锚定 / 状态合法）
- [ ] 手动验收 E1–E4（mac 实测右侧贴框稳定）
- [ ] 提交并推送，GitHub Pages 线上验证

# 附：§13 本次修改 — 光标时间标签下移到时间轴下方独立一行

版本：v1.7 追加 ｜ 日期：2026-09-01 ｜ 状态：待评审

## 13.1 背景与需求

用户反馈：**鼠标悬停时显示的光标时间（`2025-10-25 13:45:00` 黄色文本）位置太靠上，会盖住成交量柱的下沿，同时与时间轴日期标签（`10-25`）水平重叠**。

### 13.1.1 现状与根因

`draw()` 函数尾部（line ~1451-1465）：

```js
// 鼠标所在K线的日期时间（显示在十字光标竖线下方）
const hoverIdx = Math.floor(SCALE.viewStart + (hover.x - PAD_L) / SCALE.xW);
const hoverBar = getBar(hoverIdx);
if (hoverBar) {
  const tsLabel = tsStr(hoverBar[0]);
  ctx.font = '11px "SFMono-Regular", Consolas, monospace';
  ctx.textAlign = 'center';
  const tw = ctx.measureText(tsLabel).width;
  const lx = hover.x, ly = taTop - 4;     // ← 画在 taTop 上方 4px（VOL 区内）
  ctx.fillStyle = 'rgba(12,16,24,0.88)';
  ctx.fillRect(lx - tw / 2 - 5, ly - 11, tw + 10, 15);
  ctx.fillStyle = '#cdd3e0';
  ctx.fillText(tsLabel, lx, ly);
}
```

布局相关常量（line 265）：

```js
const PAD_L = 8, PAD_R = 62, PAD_T = 12, PAD_B = 32;
```

布局（draw 内）：

```js
volBot = H - PAD_B;      // 成交量底部
taTop  = volBot + 2;     // 时间轴分隔线 = H - PAD_B + 2
date label: taTop + 26   // 日期标签基线 = H - PAD_B + 28
```

代入 `PAD_B = 32`：

| 位置 | Y 坐标（H 为画布高） |
| --- | --- |
| `volBot`（成交量底） | H − 32 |
| `taTop`（时间轴分隔线） | H − 30 |
| **光标时间 `ly = taTop - 4`** | **H − 34（深入 VOL 区底部）** |
| 日期标签基线 | H − 4 |

| # | 根因 | 说明 |
|---|------|------|
| R1 | **光标时间 `ly = taTop − 4` 画在 VOL 区内** | `taTop = volBot + 2`，`taTop − 4` 即 `volBot − 2`，正好落在成交量柱最底部，与 VOL 柱子像素重叠 |
| R2 | **光标时间与日期标签水平 X 重合时上下距离仅 30 px，且都属于 `PAD_B` 底部区** | 当鼠标 X 对应的 K 线正好命中某个日期刻度（label 在 taTop+26），两者虽不严格撞线，但视觉上紧贴；VOL 柱底部也被 `fillRect(..., ly-11, ..., 15)` 背景色块直接覆盖 |
| R3 | **`PAD_B = 32` 太小，没有给光标时间留独立行** | 时间轴「刻度+日期」已经吃掉 ~28 px（H-30 到 H-4），剩 4 px 给光标时间+留白完全不够 |

### 13.1.2 影响

- 鼠标在 K 线上移动时，VOL 柱底部 2 px 持续被遮挡（条数越多越明显）
- 当鼠标 X 命中主刻度时，黄色光标时间与下方日期标签「上下贴脸」
- 用户期望：**光标时间显示在时间轴下方独立一行，与 VOL 区、日期标签完全分离；横向 X 仍跟随鼠标**，方便定位"在哪个 K 线上"

## 13.2 目标

1. 光标时间 Y 位置下移到时间轴日期标签下方，**与日期标签视觉分离 ≥ 5 px**
2. **不**遮挡任何 VOL 柱（VOL 区底部边界为 `H − PAD_B`）
3. X 仍跟随 `hover.x`（保留"指向哪根 K 线"的可读性）
4. 主图 / VOL 区高度尽量少变（≤ 20 px 收缩）

## 13.3 方案设计

```
调整常量：PAD_B  32 → 52        （底部 padding 加大 20 px，给光标时间留独立行）
调整光标 Y：ly  taTop - 4 → taTop + 42    （从 VOL 区内下移到日期标签下方）
```

布局（`PAD_B = 52`）：

| 位置 | Y 坐标 |
| --- | --- |
| `volBot`（成交量底） | H − 52 |
| `taTop`（时间轴分隔线） | H − 50 |
| 次级刻度 | H − 50 + 6 ~ + 9 |
| 主刻度 | H − 50 + 4 ~ + 14 |
| 日期标签基线 | H − 24 |
| **光标时间基线 `ly = taTop + 42`** | **H − 8（独立行，距日期标签 16 px）** |
| 光标时间背景框 | `ly − 11 = H − 19` 到 `ly + 4 = H − 4` |
| 画布底 | H |

视觉验证：

- 日期标签 baseline `H − 24` ↔ 光标时间框顶 `H − 19`：5 px 间隔（清晰分离）
- 光标时间框底 `H − 4`：距画布底 4 px（不会被裁）
- VOL 区底部 `H − 52`：与光标时间相隔 30+ px（绝不再遮挡）

| 项 | 策略 |
|---|---|
| `PAD_B` 调整 | `32 → 52`（+20 px），仅影响底部 padding，不动 `PAD_L/R/T` |
| 光标 Y | `taTop − 4 → taTop + 42`（绝对位移 +46 px） |
| 光标 X | 不变（仍跟随 `hover.x`） |
| 时间格式 | 不变（保持 `YYYY-MM-DD HH:MM:SS`） |
| 主图 / VOL 收缩 | 主图高度收缩 20 px（线性布局：`H − PAD_T − PAD_B − volH − gap`），视觉无明显感知 |

## 13.4 交互细节

- 鼠标在 K 线上：光标时间随 X 滑动，固定在画布底部独立行（不受 K 线密度/刻度影响）
- 鼠标离开画布：光标时间消失（现有 `inPlot` 判断已覆盖）
- 缩放 / 平移 / 切换周期：不受影响（光标时间基于 hover 状态实时绘制）

## 13.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| E1 | 鼠标在 1h 周期 K 线上移动 | 光标时间 `YYYY-MM-DD HH:MM` 显示在画布底部独立行，X 跟随鼠标 |
| E2 | 鼠标 X 命中某主刻度（如 `10-25`） | 光标时间与日期标签上下分离 ≥ 5 px，不水平覆盖 |
| E3 | 鼠标在 VOL 区上沿/中段 | VOL 柱不再被光标时间遮挡（VOL 区底部为 `H − 52`，光标时间框顶 `H − 19`，相距 30+ px） |
| E4 | 切换周期（15m / 1h / 4h / 1d）+ 缩放 + 平移 | 光标时间位置稳定，VOL 区不变形 |
| E5 | 对数 / 线性坐标切换 | 不影响光标时间位置 |
| E6 | 任意窗口高度（含 1080p / 4K） | 光标时间不被裁切，框底距画布底 ≥ 4 px |

## 13.6 非目标

- 不改光标时间的格式（仍 `YYYY-MM-DD HH:MM:SS`，全信息）
- 不做 X 轴越界钳制（保持现状；溢出由 canvas 边界自然截断）
- 不改 `PAD_L / PAD_R / PAD_T`（主图 / 价格轴不受本次影响）

## 13.7 测试策略

1. **自动化**：`tests/regression.test.cjs` 跑全量回归，确保：
   - PAD_B 调整不破坏任何现有断言（画线/盈亏比/缩放/平移/数据加载）
   - `draw()` 调用无异常（mock ctx 不校验视觉）
2. **手动**：mac Safari/Chrome 实测：
   - 1h / 15m 周期下移动鼠标，确认光标时间在底部独立行、与日期标签分离
   - 鼠标在 VOL 区移动，VOL 柱完整可见
   - 缩放到 1d（行内日期更长），位置仍然稳定

## 13.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | `PAD_B: 32 → 52`；光标时间 `ly: taTop-4 → taTop+42`（共 2 行改动） |
| `PRD.md` | 本文档（§13） |
| `tests/regression.test.cjs` | 无需新增断言（mock 不校验视觉） |

## 13.9 风险与对策

| 风险 | 对策 |
|---|---|
| 主图 / VOL 区收缩 20 px 影响体验 | 收缩 < 3% 高度（700 px 画布下损失 2.9%），视觉无感；如后续反馈再微调 |
| 底部 hint bar（HTML）与画布底距离拉大 | hint 在画布下方独立 HTML 元素，与 canvas 内部 padding 无关，无影响 |
| 旧 iPhone Safari 8.0.17 等小屏画布下框被裁 | `ly + 4 = H − 4` 留 4 px 余量；E6 验收覆盖 |

## 13.10 完成定义

- [x] PRD §13 写完（含验收标准 + 完成定义）
- [x] 改 `index.html`：`PAD_B` 与光标 `ly` 两处
- [x] `tests/regression.test.cjs` 全过
- [ ] mac 手动验收 E1–E6
- [x] 提交并推送，GitHub Pages 线上验证（commit `1ab8b3b`）

# 附：§14 本次修改 — 缩放降速 + 平移时 VOL 柱子不再乱跳

版本：v1.8 追加 ｜ 日期：2026-09-01 ｜ 状态：待评审

## 14.1 背景与需求

用户反馈两个问题：

1. **滚轮缩放速度太快**，希望慢一点
2. **平移（拖拽 / 双指滑动）时，成交量柱子有时候会乱跳**，询问原因

## 14.2 根因分析

### 14.2.1 问题 A：滚轮缩放太快

现状常量（`index.html` line ~2098）：

```js
const ZOOM_SENSITIVITY = 1.15;  // 一格标准滚动(120) → ×1.15
const DELTA_NORM = 120;
```

wheel 处理器（line ~2169）：

```js
wheelAccum += e.deltaY / DELTA_NORM;
...
requestAnimationFrame(() => {
  const d = wheelAccum; wheelAccum = 0;
  if (d !== 0) applyZoom(d);       // 帧内累积量一次性全应用，无上限
});
```

`applyZoom`：`factor = Math.pow(ZOOM_SENSITIVITY, delta)`

| # | 根因 | 说明 |
|---|------|------|
| R1 | **每格 15% 偏激进** | 一格滚轮（deltaY=120）即 ×1.15；连拨 3 格 → ×1.52，视图已缩小 1/3 |
| R2 | **无单帧缩放上限** | 触控板高频事件 / 快速拨轮会在单帧累积多格并**一次性全应用**；rAF 节流只合并事件、不减少总量，因此"甩一下"视图可能直接翻倍 |

### 14.2.2 问题 B：平移时 VOL 柱子乱跳（**主因**）

VOL 归一化基准是**当前可视窗口的成交量最大值**，在 `draw()` 开头按 `[s, e)` 逐根求：

```js
let vmax = 0;
for (let i = s; i < e; i++) {
  const r = getBar(i);
  if (!r) continue;
  const v = +r[5];
  if (v > vmax) vmax = v;      // ← 可视窗口内的单根最大值
}
```

柱子高度按此基准归一化（line 1342 / 1367）：

```js
// 小缩放（xW >= 1）
const vh = (v / vmax) * (volBot - volTop);
// 大缩放（xW < 1，桶聚合）
const vh = Math.min((sums[x] / maxSum) * (volBot - volTop), volBot - volTop);
```

| # | 根因 | 说明 |
|---|------|------|
| R3 | **`vmax` 随窗口滑动突变** | 平移时可视窗口 `[s, e)` 逐根滑动；一旦某根**爆量 K 线**（插针 / 爆仓 / 交割）进入或离开窗口，`vmax` 瞬间变化数倍 → 全屏柱子高度**同时反向缩放** → 视觉上就是"整屏柱子跳一下" |
| R4 | **单根离群值主导全局缩放** | 窗口 `vmax` 中位数 vs 全局最大值相差 **12.9x（1d）/ 26.8x（4h）/ 29.7x（1h）**；一根巨量 K 线就能把同屏其他柱子压到只剩 1/30 |
| R5 | **大缩放分支二次抖动** | `xW < 1` 时用桶求和的 `maxSum`；`viewStart` 为浮点而 `s = Math.floor(viewStart)` 阶跃，桶边界 `i0/i1` 随之跳动 → `maxSum` 额外抖动一层 |

**实测验证**（窗口 120 根，逐根平移，统计 `vmax` 单步相对跳变）：

| 周期 | 总根数 | 窗口 vmax 中位数 | 全局最大 | 倍数 | 单步跳变 >15% 占比 | **最大单步跳变** |
|---|---|---|---|---|---|---|
| 1d | 2546 | 935,489 | 12,105,529 | 12.9x | 1.2% (28/2424) | **2431.7%** |
| 4h | 15268 | 187,249 | 5,026,713 | 26.8x | 1.2% (176/15146) | **549.0%** |
| 1h | 61071 | 58,805 | 1,749,434 | 29.7x | 1.3% (787/60949) | **276.6%** |

结论：约 1.2% 的平移步会触发可见跳变（>15%），极端情况单步 **24 倍**——完全吻合用户"有时候会乱跳"的描述（偶发、但一旦触发非常明显）。

## 14.3 目标

1. 滚轮缩明显变慢（每格缩放幅度降至约 55%），同时保留精细可控
2. 平移 / 缩放时 VOL 柱子高度**平滑过渡**，不再整屏瞬时跳变
3. 静止（停止交互 ~0.4s 内）时柱子高度恢复准确（收敛到真实 `vmax`）

## 14.4 方案设计

### 14.4.1 缩放降速

```js
const ZOOM_SENSITIVITY = 1.08;      // 1.15 -> 1.08（每格缩放幅度降至约 55%）
const ZOOM_MAX_PER_FRAME = 3;       // 新增：单帧最大 3 格（≈26% @1.08），超出丢弃
```

wheel 处理器内钳制：

```js
let d = wheelAccum; wheelAccum = 0;
d = Math.max(-ZOOM_MAX_PER_FRAME, Math.min(ZOOM_MAX_PER_FRAME, d));   // 超出部分丢弃（不做惯性累积）
if (d !== 0) applyZoom(d);
```

| 场景 | 行为 |
|---|---|
| 慢速精细滚动（d < 3 格） | 不触发钳制，正常响应（主要场景） |
| 快速拨轮 / 触控板猛滑（d ≥ 3 格） | 钳到 3 格/帧，超出**丢弃**（非累积到下一帧，避免松手后惯性继续缩放） |
| 一格鼠标滚轮（d = 1） | ×1.08（原 ×1.15），明显更钝 |

> 保留 `ZOOM_SENSITIVITY` 为顶部常量，后续可继续微调（越小越钝）。

### 14.4.2 VOL 归一化基准平滑

新增全局状态与常量（放在 `draw()` 之前）：

```js
// §14 成交量归一化基准平滑：消除平移时单根爆量K线进出窗口导致的整屏柱子跳变
const VMAX_SMOOTH = 0.18;      // 缓动系数（0=不跟随，1=瞬时跳变）
const VMAX_SNAP_RATIO = 3;     // 偏差超过 3 倍时直接吸附（切换周期/大幅缩放不拖沓）
let volNormSmooth = 0;         // 平滑后的归一化基准（跨帧保持）
```

在 `draw()` 内，算出本帧目标基准后统一平滑：

```js
// 本帧目标归一化基准：小缩放用单根 vmax，大缩放用桶求和 maxSum
const targetNorm = (xW >= 1) ? vmax : maxSum;
// 首次 / 偏差过大 → 直接吸附；否则指数缓动
if (volNormSmooth <= 0 || targetNorm <= 0 ||
    targetNorm / volNormSmooth > VMAX_SNAP_RATIO ||
    volNormSmooth / targetNorm > VMAX_SNAP_RATIO) {
  volNormSmooth = targetNorm;
} else {
  volNormSmooth += (targetNorm - volNormSmooth) * VMAX_SMOOTH;
}
const vNorm = volNormSmooth > 0 ? volNormSmooth : targetNorm;
// 缓动未收敛则继续下一帧（保证静止时收敛到真实值）
if (Math.abs(targetNorm - volNormSmooth) > targetNorm * 1e-3) redrawSoon();
```

两个绘制分支改用 `vNorm`，且小缩放分支补 `clamp`（平滑过程中 `v` 可能短暂超过 `vNorm`）：

```js
// 小缩放
const vh = Math.min((v / vNorm) * (volBot - volTop), volBot - volTop);
// 大缩放
const vh = Math.min((sums[x] / vNorm) * (volBot - volTop), volBot - volTop);
```

切换周期 / 标的时重置（`setView` 内）：

```js
volNormSmooth = 0;   // §14：切换周期/标的后重新吸附，避免跨量纲长时间爬升
```

**收敛速度**：0.18 @ 60fps → 收敛到 99% 约 23 帧 ≈ **0.39 秒**。静止后自动精确。

## 14.5 交互细节

- **缩放**：同样滑动距离下，视图变化幅度约为原来的一半；快速滑动被自然限速
- **平移**：VOL 柱子高度平滑追随，爆量 K 线进出窗口时不再"整屏一跳"，而是 ~0.4s 内渐变到位
- **切换周期 / 标的**：直接吸附到新基准（不渐变），避免从 1h 切 1d 时慢慢爬升
- **缩放**：viewCount 变化导致聚合度变化时同样平滑，缩放过程柱子不再闪跳

## 14.6 验收标准

| # | 用例 | 预期 |
|---|------|------|
| E1 | 鼠标滚轮拨 1 格 | 视图缩放约 ×1.08（原 ×1.15），体感明显变钝 |
| E2 | 触控板快速双指滑动 | 单帧最多缩放 3 格（≈26%），不出现视图瞬间翻倍 |
| E3 | 慢速精细滚动 | 仍可精细逐格控制（不被钳制） |
| E4 | 1d 周期下拖拽平移，跨过爆量 K 线 | 柱子高度平滑过渡，**无整屏瞬时跳变** |
| E5 | 1h / 4h 周期同样平移 | 同上（1h 最大单步跳变 276.6% 的场景已消除） |
| E6 | 平移停止 ~0.4s 后 | 柱子高度收敛到准确值（与 `vmax` 一致） |
| E7 | 切换周期（15m/1h/4h/1d） | 直接吸附到新基准，渐变不超过 1 帧 |
| E8 | 缩放过程中 | 柱子高度平滑变化，不闪跳 |
| E9 | 大缩放（15m 全量 24.4 万根）平移 | 桶聚合 `maxSum` 抖动被平滑，柱子平稳 |

## 14.7 非目标

- 不改主图价格轴归一化（主图 `pmin/pmax` 抖动属另一问题，用户未反馈）
- 不改成"分位数归一化"或"对数成交量"（会改变柱子语义，需要用户确认）
- 不做 VOL 基准的跨会话持久化（每次打开重新吸附）
- 不加 UI 开关（保持界面简洁）

## 14.8 测试策略

1. **自动化**：`tests/regression.test.cjs`
   - 新增：缩放步进断言（单格缩放比例 ≈ 1.08，非 1.15）
   - 新增：VOL 平滑状态断言（`volNormSmooth` 在连续平移后单调靠近目标、最终收敛）
   - 全量回归不破坏
2. **手动**（mac 实测）：
   - 滚轮慢拨 / 快拨对比新旧手感
   - 1d 周期定位到爆量 K 线附近来回拖拽，观察柱子是否还跳
   - 切换周期观察是否瞬时到位

## 14.9 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | `ZOOM_SENSITIVITY 1.15→1.08`；新增 `ZOOM_MAX_PER_FRAME=3` 钳制；新增 `VMAX_SMOOTH/VMAX_SNAP_RATIO/volNormSmooth` 与平滑逻辑；VOL 两绘制分支改用 `vNorm` + clamp；`setView` 重置 |
| `tests/regression.test.cjs` | 缩放步进断言 + VOL 平滑收敛断言 |
| `PRD.md` | 本文档（§14） |

## 14.10 风险与对策

| 风险 | 对策 |
|---|---|
| 平滑导致柱子高度短暂失真 | 仅交互过程中（<0.4s）存在；静止即收敛。缓动未收敛时持续 `redrawSoon()` 保证精度 |
| 缩放变慢后用户觉得"拖不动" | `ZOOM_SENSITIVITY` 顶部常量，可调；3 格/帧上限在 60fps 下仍可达 ×300/秒，不限制正常快速缩放 |
| 单帧钳制丢弃累积量导致"滑了很多才动一点" | 符合用户"慢点"诉求；若后续反馈过钝，调大 `ZOOM_MAX_PER_FRAME` 即可 |
| 切换周期时跨量纲爬升 | `VMAX_SNAP_RATIO=3` 阈值直接吸附 + `setView` 显式重置，双保险 |

## 14.11 完成定义

- [x] PRD §14 写完（含实测数据 + 验收标准）
- [x] 改 `index.html`：缩放常量 + 钳制 + VOL 平滑（含 `setView` 重置）
- [x] `tests/regression.test.cjs` 新增断言并全过（114 PASS / 0 FAIL）
- [ ] mac 手动验收 E1–E9
- [x] 提交并推送，GitHub Pages 线上验证（commit `e8247a5`）

# 附：§15 本次修改 — 辅助线不再遮挡成交量柱子

版本：v1.9 追加 ｜ 日期：2026-09-01 ｜ 状态：待评审

## 15.1 背景与需求

用户反馈：**画的水平线和一些辅助线（趋势线 / 通道 / 测量线），有时候会挡住成交量柱子**，
询问原因。

### 15.1.1 现状与根因

布局分两个区域（`draw()` line 1205-1206）：

```js
const mainTop = PAD_T, mainBot = PAD_T + (H - PAD_T - PAD_B - volH - gap);
const volTop = mainBot + gap, volBot = H - PAD_B;
// 主图价格区：[mainTop, mainBot]；成交量子面板：[volTop, volBot]
```

`drawLines()`（line 1526）绘制 hline / trend / channel / measure，**用 `priceToY(price)` 取 y、整绘图宽绘制、且完全没有裁剪**，并且它在 `draw()` 内被调用（line 1458）**晚于 VOL 绘制**（line ~1340），所以线条会直接盖在 VOL 柱子上。

| # | 根因 | 说明 |
|---|------|------|
| R1 | **辅助线绘制无裁剪** | `drawLines()` 全程不调用 `ctx.clip()`，线条按 `priceToY` 算出的 y 直接画满整宽，会落到主图区以外的像素 |
| R2 | **价格低于可视最小价时 y 落入 VOL 区** | 当某条线的 `price` 低于当前可视窗口最小价（含 padding 的 `pmin`）时，`priceToY(price)` 返回的 `y > mainBot`，进入 `[volTop, volBot]` 区间，与成交量柱像素重叠 |
| R3 | **调用顺序：线在 VOL 之后** | `drawLines()`（1458）在 VOL（~1340）之后绘制，本就应是"覆盖层"，但缺少区域边界，导致覆盖到了 VOL 子面板 |
| R4 | **趋势/通道/测量线同理** | 这些线两端按价格外推到绘图左右边缘，只要任一端价格越界（低于 pmin 或高于 pmax），对应端点 y 落入 VOL 区即遮挡柱子（高于 pmax 时落在顶部 padding，无碍） |

### 15.1.2 影响

- 把一条水平线画在"价格区间下方"（例如贴着底部、或画在一段深跌后的低位），鼠标或自动绘制出的辅助线会横跨成交量柱，遮住柱高
- 趋势线/通道线/测量线若一端锚定在低价位，另一端延伸到绘图右缘时若价格线整体偏低，同样压住 VOL
- 用户期望：**辅助线只出现在主图价格区内，绝对不进入成交量子面板**

## 15.2 目标

1. 所有辅助线（hline / trend / channel / measure / trade / RR 草稿）**只渲染在主图价格区 `[mainTop, mainBot]` 内**
2. **任何价格下都不遮挡成交量柱**（线 y 触及 VOL 区即被裁掉，而非覆盖）
3. 主图内部显示完全不变（线在主图区内的位置、颜色、标签、手柄一切照旧）
4. 缩放 / 平移 / 切换周期 / 画线交互全部不受影响

## 15.3 方案设计

在 `draw()` 内，对**价格类覆盖层**统一做区域裁剪——把 `drawLines()`、`drawTrades()`、`drawRRDraft()` 三个调用包进一次 `save` + `clip` + `restore`：

```js
// §15：价格类覆盖层裁剪到主图区，绝不进入成交量子面板
ctx.save();
ctx.beginPath();
ctx.rect(PAD_L, mainTop, plotW, mainBot - mainTop);   // 仅主图价格区 [mainTop, mainBot]
ctx.clip();
drawLines();
drawTrades();
drawRRDraft();
ctx.restore();
```

| 项 | 策略 |
|---|---|
| 裁剪区域 | `[PAD_L, mainTop, plotW, mainBot - mainTop]`——严格等于主图价格区，下边界 `mainBot` 距 VOL 起点 `volTop` 还留 `gap`(8px) 余量 |
| 受影响调用 | `drawLines()`（hline/trend/channel/measure）、`drawTrades()`（入场/止盈/止损线）、`drawRRDraft()`（拖拽中的盈亏比三线）——均为价格锚定覆盖层 |
| 十字光标（crosshair） | **不裁剪**：光标竖线本就贯穿主图+VOL（line 1467-1471），属交互指示，用户未反馈，保持不变 |
| 时间轴 / VOL / 主图 K 线 | 在裁剪之前已绘制，不受影响 |
| 标签/手柄 | 落在主图区外的端点标签/手柄随裁剪一并隐藏（价格已越界、属离屏内容，隐藏正确） |

> 不采用"把线移到 VOL 之前绘制让柱子盖住线"的方案：VOL 柱间有空隙，线会在间隙"漏出"，视觉更乱；裁剪是唯一干净做法。

## 15.4 交互细节

- 水平线画在价格区间下方 → 越界部分被裁，主图内可见、VOL 区干净
- 趋势/通道/测量线一端在低价位 → 该侧延伸段被裁，不再压柱
- 缩放/平移使线重新进入可视价格区 → 立即重新完整显示（裁剪跟随每帧 `mainTop/mainBot`）
- 切换周期 → 主图高度微变，裁剪区随之调整，无残留

## 15.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| E1 | 画一条远低于当前最低价的水平线 | 主图区内可见，VOL 柱完全不被遮挡 |
| E2 | 画一条贴着底部的水平线 | 整条线在 `mainBot` 以下的部分被裁掉，VOL 区无横向色线 |
| E3 | 趋势线一端锚定低价位、延伸到右边缘 | 低价侧延伸段被裁，不压 VOL 柱 |
| E4 | 通道线（含半透明填充） | 填充与边线均不进入 VOL 区 |
| E5 | 测量线一端在低价位 | 该端标签/线段被裁，不遮挡 VOL |
| E6 | 缩放 / 平移 / 切换周期 | 裁剪随 `mainTop/mainBot` 实时生效，无残留线条 |
| E7 | 十字光标 | 仍贯穿主图+VOL（不被本次裁剪影响） |

## 15.6 非目标

- 不改辅助线的颜色 / 线型 / 标签格式
- 不改十字光标行为（继续贯穿 VOL）
- 不做"线在 VOL 区改画成别的形式"等额外表现

## 15.7 测试策略

1. **自动化**：`tests/regression.test.cjs` 全量回归必须保持通过（mock ctx 不校验视觉，但需确保 `draw()` 调用不抛错、clip 包裹不破坏既有断言）
2. **手动**（mac 实测）：§15.5 E1–E7

## 15.8 交付物

| 文件 | 内容 |
|---|---|
| `index.html` | `draw()` 内对 `drawLines/drawTrades/drawRRDraft` 三个调用加 `save`+`clip([PAD_L,mainTop,plotW,mainBot-mainTop])`+`restore` |
| `PRD.md` | 本文档（§15） |
| `tests/regression.test.cjs` | 无需新增断言（裁剪不影响逻辑断言）；全量回归保持通过 |

## 15.9 风险与对策

| 风险 | 对策 |
|------|------|
| 裁剪误伤主图内正常线条 | 裁剪区 = 主图真实区 `[mainTop, mainBot]`，区内绘制与现状完全一致 |
| 标签/手柄被裁导致操作困难 | 被裁的端点本就价格越界（离屏），正常交互只发生在可视区，无碍 |
| 十字光标需求变化 | 明确排除在裁剪外，保持贯穿行为 |
| 回归风险 | 仅改 `draw()` 一次调用包裹；跑全量 regression |

## 15.10 完成定义

- [x] PRD §15 写完（含根因 + 验收标准 + 完成定义）
- [x] 改 `index.html`：`draw()` 内裁剪包裹三调用
- [x] `tests/regression.test.cjs` 全过（114 PASS / 0 FAIL 保持）
- [ ] mac 手动验收 E1–E7
- [x] 提交并推送（`git ls-remote origin main` 确认远端 = 本地 `82c0216`）；GitHub Pages 自动部署待线上验证

# 附：§16 本次修改 — 缩小后平移成交量/蜡烛"冻住→整批跳"的离散跳变

版本：v1.10 追加 ｜ 日期：2026-09-01 ｜ 状态：待评审

## 16.1 背景与需求

用户反馈：**缩小（zoom-out，xW<1）后平移 K 线，成交量柱子仍"跳来跳去"**，希望平稳过渡。

## 16.2 根因分析

§14 已做"归一化基准（`vmax`/`maxSum`）指数平滑"，消除了"爆量 K 线进出窗口 → 基准突变数倍 → 全屏反向缩放"的跳变。但缩小后平移仍有另一层跳变，根因在**桶聚合的坐标基准**：

`draw()` 内 `bucketOf`（line 1262）基于**整数 `s = floor(viewStart)`** 计算桶边界：

```js
const bucketOf = (x) => ({ i0: s + Math.floor(x * nVis / Wpx), i1: s + Math.floor((x + 1) * nVis / Wpx) });
```

主图蜡烛（xW<1 分支）与成交量都使用它。问题在于：**平移时 `s` 只在 `viewStart` 跨过整数时 +1**，于是：

- `s` 不变的一段里，所有桶范围固定 → 成交量柱 / 蜡烛"冻住不动"
- `viewStart` 每跨 1 个整数边界，`s` +1 → 全部桶整体平移 1 根 → "整批跳一下"

zoom-out 时 `xW` 极小（如 0.01 px/根），**每拖 1px 鼠标 `viewStart` 变化 ≈ 1/xW ≈ 100 根** → `s` 一次跳约 100 → 成交量窗口被整体替换 → 视觉上就是"跳来跳去"；蜡烛因只画 1px 高低线、轮廓自相似，看起来像平滑滚动，所以用户只注意到成交量在跳。

| # | 根因 | 说明 |
|---|------|------|
| R1 | **`bucketOf` 用整数 `s` 而非连续 `viewStart`** | 桶边界离散跳转（每整数边界整批 +1 根），平移时 K 线 / 成交量非连续滑动 |
| R2 | **zoom-out 单 px 拖动偏移数十~上百根** | `d(viewStart)/dx = 1/xW`，xW 越小每 px 偏移越多，`s` 跳变幅度被放大 → 跳变肉眼明显 |
| R3 | **§14 平滑只压了"基准"，没压"窗口滑动"** | `vNorm` 平滑的是 `maxSum`（分母）；分子 `sums[x]` 随离散桶整体替换而跳变，分母平滑救不回 |

## 16.3 目标

1. 缩小后平移，成交量柱与蜡烛**连续滑动**（与缩放级别无关的平滑滚动）
2. 不引入新的视觉错位（成交量仍与主图蜡烛对齐）
3. 缩放 / 平移 / 切周期 / 画线交互不受影响
4. §14 的"爆量 K 线进出窗口"平滑仍保留

## 16.4 方案设计

把 `bucketOf` 的坐标基准从整数 `s` 改为连续 `viewStart`：

```js
const vs0 = viewStart;   // §16：连续基准，桶窗口随 viewStart 连续滑动
const bucketOf = (x) => {
  const i0 = Math.max(0, Math.floor(vs0 + x * nVis / Wpx));
  const i1 = Math.min(len, Math.floor(vs0 + (x + 1) * nVis / Wpx));
  return { i0, i1 };
};
```

| 项 | 策略 |
|---|---|
| 坐标基准 | `s` → `viewStart`（连续浮点），桶边界随平移**连续**滑动，不再"冻住→整批跳" |
| 跨度 | `nVis = e - s` 不变；`vs0 + nVis ≈ e`（右边缘对齐最新 / 锚定边界），桶覆盖 `[vs0, ≈e]` 与蜡烛循环 `[s, e]` 一致 |
| 蜡烛 | xW<1 分支同样用 `bucketOf` → 蜡烛也连续滚动（此前也离散，只是 1px 线不易察觉，现一并修复） |
| 越界 | `i0` 下限 0、`i1` 上限 `len`，与现状一致 |
| §14 平滑 | 保留：`maxSum` 随连续桶平滑变化，缓动进一步消除残余跳变；静止收敛不变 |

## 16.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| E1 | 缩小到 15m 全量，按住拖拽平移 | 成交量柱随鼠标**连续滑动**，无"冻住→整批跳" |
| E2 | 同上，观察蜡烛 | 蜡烛高低线同步连续滚动，与成交量对齐 |
| E3 | 慢速精细平移 | 每 px 偏移与蜡烛一致，平滑无台阶 |
| E4 | 缩放到中段（非全量）平移 | 同样连续，右边缘锚定不漂移（§12 保持） |
| E5 | 缩小后停手 | 约 0.4s 内 `vNorm` 收敛，柱子高度准确（§14 保持） |
| E6 | 缩放过程中 | 桶聚合平滑，无闪跳 |
| E7 | 切周期 / 切标的 | 直接吸附（§14 `setView` 重置），无爬升 |

## 16.6 非目标

- 不改主图价格轴归一化
- 不改 §14 的 `vNorm` 平滑参数（如需更跟手可后续调 `VMAX_SMOOTH`）
- 不做成交量"对数 / 分位数"归一化（会改变语义，需用户确认）

## 16.7 测试策略

1. **自动化**：`tests/regression.test.cjs` 全量回归保持通过（mock ctx 不校验桶边界视觉；确保 `draw()` 调用不抛错）
2. **手动**：mac 实测 §16.5 E1–E7

## 16.8 交付物

| 文件 | 内容 |
|---|---|
| `index.html` | `bucketOf` 改用连续 `viewStart`（1 行函数体改动） |
| `PRD.md` | 本文档（§16） |
| `tests/regression.test.cjs` | 无需新增断言；全量回归保持通过 |

## 16.9 风险与对策

| 风险 | 对策 |
|------|------|
| 连续桶导致蜡烛与成交量轻微错位 | 两者共用同一 `bucketOf`，天然对齐；`vs0 + nVis ≈ e` 与蜡烛循环一致 |
| 右边缘锚定受影响 | `vs0 + nVis ≈ e`，桶覆盖到最新 / 锚定边界，§12 行为不变 |
| 缩小时每 px 偏移仍大（xW 极小） | 连续滑动后各帧内容差 < 1px（与蜡烛同速率），不再整批替换 |
| 回归风险 | 仅改 `bucketOf` 一行；跑全量 regression |

## 16.10 完成定义

- [x] PRD §16 写完（含根因 R1–R3 + 验收标准 + 完成定义）
- [x] 改 `index.html`：`bucketOf` 连续化
- [x] `tests/regression.test.cjs` 全过（114 PASS / 0 FAIL 保持）
- [ ] mac 手动验收 E1–E7
- [x] 提交并推送（commit `d5e767e` → `origin/main`，`git ls-remote` 确认远端 = 本地）；GitHub Pages 自动部署，curl 确认线上含 `vs0 = viewStart` / `VMAX_SMOOTH`、`BTCFUT_DATA` 15m 244280 根，部署成功

# 附：§17 本次修改 — 缩放锚定最新K线（在屏→钉最新贴右框；离屏→锚当前右边缘；缩到全量即停）

版本：v1.12 修订 ｜ 日期：2026-09-01 ｜ 状态：待评审（修订：离屏不再强拉回最新）

## 17.1 背景与需求

用户初版反馈：**缩放时希望最新一根 K 线固定（贴右边缘），不要露出它"之后"的区域（未来/空白），
并且缩小到不能再缩（显示全部数据）就停止。**

修订反馈：但**如果拖动画面让最新一根 K 线离开了屏幕（平移到历史区间），缩放时就不该强行把最新根拉回右边缘**——
此时应锚定到"当前可视区域的右边缘"，保持用户所在的历史视图，便于在历史区间里继续缩放探索。
即：`最新在屏 → 钉最新（贴右框、不露未来）；最新离屏 → 锚当前右边缘（不拉回最新）`。

## 17.2 根因分析

`applyZoom`（index.html line 2144）当前锚点是**当前右边缘**：

```js
const rightEdge = viewStart + viewCount;   // 缩放前右边缘作为锚点（=最新K线侧，固定不动）
viewCount = nc;
viewStart = Math.max(0, Math.min(len - viewCount, rightEdge - viewCount));
```

§12 的注释写明意图是"右侧（最新K线）始终钉住，避免露出右侧未来空白"，但实现用的是
`viewStart + viewCount`（**当前**右边缘），只有当你本来就停在最新时它才等于最新；一旦**先平移到历史区间再缩放**，锚点就是你当时的右边缘，最新一根始终在屏幕右侧之外、回不来。

Node 仿真（`/tmp/zoom_sim.cjs`，真参数）结论：

| 场景 | 起始 | 缩小若干次后 rightEdge | 最新在右边缘 |
|---|---|---|---|
| A：默认（最新已贴右） | vs=len-200, vc=200 | 恒 = `len` ✅ | 是 |
| B：平移到历史 vs=1000 | vs=1000, vc=200 | 恒 = `1200`（旧右边缘）❌ | 否（最新在屏外右侧） |
| C：默认放大 | vs=len-200, vc=200 | 恒 = `len` ✅ | 是 |

即：**停在最新时一切正常；一旦平移过，缩放锚定在旧位置，最新回不来**——正是用户感知到的"最新没固定"。

| # | 根因 | 说明 |
|---|------|------|
| R1 | **缩放锚点用"当前右边缘"而非"最新一根"** | `rightEdge = viewStart + viewCount` 在平移态锚定到旧位置，最新 K 线无法在缩放时回到右边缘 |
| R2 | **§12 注释意图未落到实现** | 注释要"最新K线始终钉住"，实现只钉住了"你当时所在位置的右边缘" |

## 17.3 目标

1. **最新一根在屏内**时，任何缩放（放大/缩小）后最新一根恒贴右边缘
2. **最新一根离屏（已平移到历史）**时，缩放锚定到"当前可视右边缘"，保持历史视图、不强行拉回最新根
3. **绝不露出最新一根右侧的未来/空白区**（rightEdge ≤ `len`）
4. 缩小到全量（`viewCount = len`、`viewStart = 0`）即停止，不再继续缩出右侧空白
5. 平移（不缩放）仍可自由看历史，仅缩放操作触发上述锚定语义
6. 缩放灵敏度（§14）、VOL 平滑（§14）、辅助线裁剪（§15）、成交量连续滑动（§16）全部不受影响

## 17.4 方案设计

`applyZoom` 的锚点改为**条件锚定**：先判断"当前右边缘是否已抵达末根 `len`"（即最新一根是否仍在屏内），
再决定锚到 `len` 还是当前右边缘：

```js
// §17：缩放锚点分两种情形——
//   1) 最新一根仍在屏内（当前右边缘已抵达末根 len）→ 钉住最新K线（贴右框、不露未来空白）；
//   2) 已拖拽到历史区间（最新一根离开屏幕，curRight<len）→ 锚定「当前右边缘」，不再强行拉回最新根。
const curRight = viewStart + viewCount;
const rightEdge = (curRight >= len) ? len : curRight;
viewCount = nc;
viewStart = Math.max(0, Math.min(len - viewCount, rightEdge - viewCount));
```

| 项 | 策略 |
|---|---|
| 锚点 | `viewStart + viewCount` → 条件：`(curRight >= len) ? len : curRight` |
| 在屏（curRight≥len） | 缩放后 `viewStart = len - viewCount`，`rightEdge = len` 恒成立（最新恒贴右框、不露未来） |
| 离屏（curRight<len） | 缩放后 `viewStart = curRight - viewCount`，`rightEdge = curRight` 保持不变（历史视图不跳、最新不拉回） |
| 不露未来空白 | 两种情形下 `rightEdge ≤ len`（末根或更小），右侧无空白/未来区 |
| 缩到全量即停 | 离屏时持续缩小→`vc` 增长、当 `vc>curRight` 后 `viewStart` 钳到 0、最终 `vc=len,viewStart=0`，最新回到右框；不超出 |
| 平移不受影响 | 仅 `applyZoom` 改；`applyPan`（双击拖拽 / 双指滑动）逻辑不变，仍可看历史 |
| §12 桶对齐 | 量化分支仅在 `vsQ + viewCount === rightEdge` 时生效；在屏时 rightEdge=len（通常非整桶→不量化），离屏时 rightEdge=curRight（量化仅当 viewStart 恰整桶→不变）；柱稳定已由 §16 连续 `bucketOf` 保证 |

## 17.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| E1 | 默认（最新贴右）缩小/放大 | rightEdge 恒 = `len`，最新贴右框 |
| E2 | **平移到历史区间（最新离屏）后缩放** | 缩放锚定"当前右边缘"，rightEdge 保持不变（≈ 缩放前），最新根**不**被拉回；最新仍不在屏 |
| E3 | 缩小到极限（无论是否在屏） | `viewCount = len`、`viewStart = 0`，rightEdge = `len`，无右侧空白 |
| E4 | 放大（任何起点） | 在屏→最新恒贴右边缘；离屏→右边缘保持当前值 |
| E5 | 缩小过程中 | 任意中间档位 rightEdge ≤ `len`，不露未来区；在屏则恒=`len`、离屏则保持当前右边缘 |
| E6 | 仅平移（不缩放） | 仍可自由看历史（applyPan 不变） |
| E7 | 缩放 + 其他功能 | 灵敏度(§14)/VOL平滑(§14)/辅助线(§15)/成交量连续滑动(§16) 全部正常 |

## 17.6 非目标

- 不改平移（拖拽 / 双指）的看历史能力
- 不改缩放灵敏度参数（§14）
- 不做"缩放时同时钉住左边缘/居中"等额外语义

## 17.7 测试策略

1. **自动化**：`tests/regression.test.cjs` 新增/修订 §17 断言：
   - 在屏（最新贴右）缩放 → rightEdge 恒 = `len`
   - 离屏（平移到历史）：单次缩放右边缘保持（不拉回最新、最新仍不在屏）
   - 缩小到极限 → `viewCount === len`、`viewStart === 0`、rightEdge = `len`
   - 全量回归保持（§5.9 场景1 改为"单次缩放右边缘稳定"；§5.5 加"单次缩放右边缘保持"）
2. **手动**：mac 实测 §17.5 E1–E7

## 17.8 交付物

| 文件 | 内容 |
|---|---|
| `index.html` | `applyZoom` 锚点 `viewStart+viewCount` → `len`（1 行常量） |
| `tests/regression.test.cjs` | 新增 §17 断言；§12 右边缘断言更新为"缩放后 ≡ len" |
| `PRD.md` | 本文档（§17） |

## 17.9 风险与对策

| 风险 | 对策 |
|------|------|
| 离屏缩放"跳回最新" | 已按用户修订改为条件锚定：仅最新在屏时钉最新，离屏时锚当前右边缘（不再跳回） |
| §12 既有断言失效 | 更新为"缩放后 rightEdge ≡ len"（更贴合新语义） |
| 柱跳动回归（失去桶对齐量化） | §16 连续 `bucketOf` 已保证柱随 viewStart 连续滑动，无需 §12 量化 |
| 回归风险 | 仅改 `applyZoom` 一行；跑全量 regression |

## 17.10 完成定义

- [x] PRD §17 写完（v1.12 修订：含两情形锚定方案 + 验收标准 + 完成定义）
- [x] 改 `index.html`：`applyZoom` 锚点改条件 `(curRight>=len)?len:curRight`
- [x] `tests/regression.test.cjs` §17 两情形断言 + §5.9/§5.5 断言更新，全过（123 PASS / 0 FAIL）
- [ ] mac 手动验收 E1–E7
- [ ] 提交并推送（本轮修订待推送）

### 附：顺带修复（本次一并完成）

测试里 2 条**硬编码数据快照**断言，因数据每日自动更新必然失效，改为持久性校验：

| 原断言 | 问题 | 改为 |
|---|---|---|
| `15m 根数 === 243961` | 数据已增长到 244280，必然 FAIL | 量级 `>240000` + 周期比例 `15m:1h≈4:1`、`15m:1d≈96:1` |
| `末根 === 2026-08-23 23:45` | 末根随时间推移，必然 FAIL | 15 分钟对齐 + 落在 `[2019-09-08, 现在+1天]` 区间 |

改后仍可捕捉数据被清空/损坏，但不再随每日同步失效。
