# PRD：BTC 时光机 — 数据同步（k--data 仓库）

版本：v1.1 ｜ 日期：2026-08-14 ｜ 状态：待评审

> **v1.1 修改记录**：在本文档基础上追加 **§8「macOS 滚轮缩放灵敏度优化」**（2026-08-14 新增）。
> 原 §1–§7 数据同步内容保持不变。

## 1. 背景与目标

`btc-timeslicer`（BTC 时光机）为单文件复盘工具，数据内联于 `index.html`（`window.BTCFUT_DATA`），
当前截止 **2026-07-28**，已过期约半个月。

另有数据仓库 `github.com/sdlyingyong/k--data`（master 分支，**已更新至 2026-08-12**），
其 BTC 数据与 `btc-timeslicer` **完全同源**（币安 BTCUSDT 永续；已实测重叠 1370 根 15m K 线
O/C 零差异）。

目标：从 `k--data` 拉取最新 BTC 数据，同步重建 `btc-timeslicer` 的内联数据，
使页面数据更新至 2026-08-12（或 k--data 最新提交）。

## 2. 数据源与格式对照

| 项 | k--data（源） | btc-timeslicer（目标） |
| --- | --- | --- |
| 位置 | `btc_fut_data.js` + `fut_data/btc_15m_*.json` | `index.html` 内 `window.BTCFUT_DATA` |
| 时间格式 | 字符串 `"YYYY-MM-DD HH:MM:SS"`（UTC） | 数字：分钟 epoch（`t = ms/60000`） |
| 字段 | 9 元 `[t,o,h,l,c,v,quoteV,chg%,""]` | 6 元 `[t,o,h,l,c,v]` |
| 周期 | 15m 分块（chunkSize=20000）+ 1h/4h/1d 内联 + 5m | 1d/4h/1h/15m 全部内联 |
| 截止 | 2026-08-12 23:45（15m） | 2026-07-28 00:00 |

- 15m 根数：k--data 242905 vs 当前内联 241370（差 1535 ≈ 07-28→08-12 的 15m 根数，吻合）
- 成交量 v 均为币安 base 口径（BTC），无需换算
- 同源性已验证：重叠区间 1370 根 15m 逐根 O/C 完全一致

## 3. 同步方案

### 3.1 数据获取（二选一）
1. **本地模式**（推荐）：读取本地 `k--data` 仓库目录（存在则 `git pull`，不存在则 clone）
2. **远程模式**：经 `gh-proxy.com` 或直连 GitHub 拉取 `master` 分支的 `btc_fut_data.js` 与 13 个 15m 分块

### 3.2 转换与重建
1. 15m：分块按序拼接 → 时间戳转分钟 epoch → 截取 9 字段前 6 项
2. 1h/4h/1d：**从 15m 序列重采样**（O=时段首根 O，H/L 取极值，C=末根 C，V 求和）
   - 周期对齐：按 UTC 整界（60/240/1440 分钟）
3. 整体替换 `window.BTCFUT_DATA`，重建 `index.html`（其余代码零改动）
4. 保留文件体积：单文件 ~16MB 不变

### 3.3 校验（通过方可提交）
- 15m 时间连续：相邻间隔恒 = 15 分钟（允许 0 间隔=重复，报错）
- 各周期时间戳严格递增、无重复
- 重采样 1h/4h/1d 与 k--data 内联数组逐根核对（O/H/L/C/V 全等）
- 末根时间 = k--data 的 `last`（15m: 2026-08-12 23:45）
- 新旧数据重叠段（≤2026-07-28）15m 逐根一致（保证不篡改历史）

## 4. 已知差异（如实说明）

- **1d/4h/1h 的开盘价 O 将被修正**：现内联数据 O 采用「前收」惯例（O=前一根 C），
  k--data（币安真实数据）O 为时段内真实开盘价。重采样后历史 O 会变化（H/L/C/V 不变）。
  实测例：07-27 日线 O 65375.2 → 63662.7。**属数据修正而非回归**，PRD 记录此变更。
- 页面上线后数据仍会过期：本次同步为**一次性脚本同步**（与 k--data 更新节奏一致），
  实时在线更新（交易所直连）不在本期范围。

## 5. 交付物

| 文件 | 内容 |
| --- | --- |
| `sync_from_kdata.cjs` | 同步脚本（Node，无依赖）：拉取→转换→重采样→重建→校验 |
| `index.html` | 数据更新至 2026-08-12 |
| `README.md` | 数据来源与同步方式说明 |
| `PRD.md` | 本文档 |

## 6. 验收标准

1. 运行 `node sync_from_kdata.cjs`（本地模式）成功重建 `index.html`
2. 校验全通过（见 3.3），15m 末根 = 2026-08-12 23:45
3. 浏览器打开页面：四周期切换正常，最新 K 线显示 08-12，画线/记忆功能不受影响
4. `git commit + push` 后 GitHub Pages 线上数据同步至最新

## 7. 非目标（本期不做）

- 接入 k--data 的 5m/ETH/上证数据（btc-timeslicer 仅 BTC 15m 基底）
- 自动定时同步（GitHub Actions / cron）
- 浏览器端在线增量更新（此前 PRD 讨论过的 OKX 直连方案，本期不做）

---

# 附：§8 本次修改 — macOS 滚轮缩放灵敏度优化

版本：v1.1 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 8.1 背景与问题

用户反馈：**在 macOS 上滚轮缩放过于灵敏，不好用**。

### 8.1.1 现状代码（根因定位）

`index.html` 滚轮缩放处理（已按本 PRD 重写，见下方实现）：

```js
// 重写后：delta 累积 + 指数缩放 + rAF 节流
const ZOOM_SENSITIVITY = 1.15;
const DELTA_NORM = 120;
let wheelAccum = 0, wheelScheduled = false;
function applyZoom(delta) { /* factor = Math.pow(ZOOM_SENSITIVITY, delta); ... */ }
canvas.addEventListener('wheel', e => {
  e.preventDefault();
  wheelAccum += e.deltaY / DELTA_NORM;   // 归一化累积
  if (wheelScheduled) return;
  wheelScheduled = true;
  requestAnimationFrame(() => { wheelScheduled = false; ... applyZoom(wheelAccum); });
}, { passive: false });
```

修复前根因（已解决）：

| # | 根因 | 说明 |
|---|------|------|
| R1 | **固定百分比步进** | ~~每次滚轮事件缩放 15%（放大）或 13%（缩小），不随滚动幅度变化~~ → 改为幅度参与换算 |
| R2 | **未利用 `deltaY` 幅度** | ~~无论滚 1 格还是滚 10 格，缩放量完全一样~~ → 按 `deltaY/DELTA_NORM` 归一化后指数换算 |
| R3 | **无节流 / 无累积** | ~~每个 wheel 事件直接重算重绘~~ → rAF 帧内累积、帧末一次性生效 |
| R4 | 方向与系统滚动方向耦合 | 经分析现有语义（上推=放大、下滚=缩小）与主流图表惯例一致，**方向保持不变** |

### 8.1.2 影响

- 触控板用户轻扫即大幅缩放，无法停在目标区间
- 高频事件导致缩放抖动、难以回看特定 K 线区域
- 方向反直觉（取决于系统滚动方向设置）

## 8.2 目标

在**不改变现有功能**（画线、平移、记忆、各周期切换）的前提下：

1. 缩放**平滑、可精细控制**：滚动幅度与缩放量成正比
2. 触控板高频事件**累积后节流**，避免逐事件跳动
3. 方向**跟随系统滚动方向设置**（macOS「自然滚动」开/关均符合直觉）
4. 键盘步进（←/→）、拖动平移、多周期切换等现有交互**保持不受影响**

## 8.3 方案设计

### 8.3.1 核心改动（`index.html` wheel 处理器重写）

采用 **delta 累积 + 指数缩放 + rAF 节流** 的标准做法：

```
1. wheel 事件不直接缩放，而是把 deltaY 累积进 accumulator
2. 每帧（requestAnimationFrame）读取累积值，换算缩放步进：
   factor = Math.pow(ZOOM_SENSITIVITY, normalizedDelta)   // 连续、非固定步进
3. 换算后重置 accumulator，执行一次重绘（rAF 天然节流）
4. 方向：读系统设置判定是否「自然滚动」，决定 deltaY 符号是否取反
```

关键参数：

| 参数 | 默认 | 说明 |
|------|------|------|
| `ZOOM_SENSITIVITY` | 1.15 | 指数底数；一格标准滚动(120)的缩放倍数，与原始单事件手感基准一致；越小越"钝"（越不灵敏），可调 |
| `DELTA_NORM` | 120 | 单格标准 delta（鼠标一格=120），用于归一化触控板/鼠标 |
| 累积窗口 | rAF 帧 | 帧内累积，帧末一次性生效 |

### 8.3.2 缩放锚点保持

沿用现有逻辑：**缩放时右边缘（最新 K 线侧）固定不动**，只改变左侧可见历史量；
桶对齐（`viewStart` 量化到桶边界）保持不变，避免柱子跳动。

### 8.3.3 边界与钳制

沿用现有：`viewCount` 钳制在 `[20, len]`，`viewStart` 钳制在 `[0, len-viewCount]`。

### 8.3.4 兼容性

- `preventDefault` 保留（阻止页面滚动/缩放冲突）
- `passive: false` 保留（必须，否则 preventDefault 无效）
- 鼠标滚轮（每格 120）与触控板（小 delta 高频）经归一化后表现一致

## 8.4 交互细节

- **触控板**：轻滚 → 小幅度连续缩放；快速长滚 → 快速缩放（按幅度自然放大）
- **鼠标滚轮**：一格 = 标准幅度缩放（与原来接近，但连续而非跳变）
- **方向**：macOS「自然滚动」开启时，双指上推（deltaY 为负）应**放大**（符合用户预期）
- 不改动：画线工具、拖动平移、←/→ 步进、G 跳转、Del 删除、盈亏比拖拽

## 8.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| A1 | 触控板轻滚一下 | 缩放幅度小且连续，可停在目标区间 |
| A2 | 触控板快速长滚 | 缩放加速但平滑，无逐事件跳变 |
| A3 | 鼠标滚轮一格 | 缩放一档，方向与幅度合理 |
| A4 | 系统「自然滚动」开/关各测一次 | 方向均符合直觉（上推放大/上推缩小随设置） |
| A5 | 缩放后右边缘最新 K 线 | 保持固定不漂移 |
| A6 | 缩放范围 | 最小/最大钳制不越界，不白屏不报错 |
| A7 | 缩放后画线/平移/记忆 | 现有功能全部正常 |
| A8 | 性能 | 高频滚动下帧率稳定，无卡顿（rAF 节流生效） |

## 8.6 非目标（本次修改不做）

- 不改动画线、平移、记忆、周期切换等其他交互
- 不做双击缩放 / 按钮缩放 / 快捷键缩放（可后续）
- 不做 Windows 专项（此改动对 Windows 同样受益，但以 mac 为主要验证环境）
- 不做缩放平滑动画插值（保持即时响应）

## 8.7 测试策略

1. **手动测试**（主）：macOS 触控板 + 鼠标各跑一遍 §8.5 验收清单
2. **回归**：现有 `tests/regression.test.cjs` 必须全过（保证画线/数据/记忆无回归）
3. **参数可调**：`ZOOM_SENSITIVITY` 提取为常量，便于后续按反馈调参

## 8.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | 重写 wheel 处理器（累积 + 指数缩放 + rAF 节流；方向保持） |
| `tests/regression.test.cjs` | 新增 wheel 缩放回归断言（方向/幅度/锚定/钳制，共 9 项） |
| `PRD.md` | 本文档（v1.1，含本修改章节） |

## 8.9 风险与对策

| 风险 | 对策 |
|------|------|
| rAF 累积导致缩放延迟（手感"肉"） | 帧间隔极短（16ms），感知可忽略；若需更跟手可降为「事件触发 + 节流」混合 |
| 系统滚动方向 API 兼容性 | 经分析现有方向语义符合图表惯例，方向不变（无需依赖系统 API） |
| 灵敏度参数不适配所有用户 | 参数常量化（`ZOOM_SENSITIVITY`）+ 文档说明，后续可加 UI 滑块 |
| 回归风险 | 只改 wheel 处理器一个函数；跑全量 regression 测试 |

## 8.10 完成定义

- [x] 自动化测试：`tests/regression.test.cjs` **64/64 通过**（基线 55 + wheel 新增 9），覆盖方向/幅度/锚定/钳制
- [ ] §8.5 手动验收 A1–A8（macOS 触控板/鼠标实机手感）
- [x] 代码修改已落地（wheel 处理器重写）
- [ ] 提交并推送，GitHub Pages 线上验证缩放手感
