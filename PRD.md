# PRD：BTC 时光机 — 数据同步（k--data 仓库）

版本：v1.13 ｜ 日期：2026-08-14 ｜ 状态：待评审

> **v1.1 修改记录**：追加 **§8「macOS 滚轮缩放灵敏度优化」**（2026-08-14，已实现并推送 `bf4eaf8`）。
> **v1.2 修改记录**：追加 **§9「触控板双指滑动平移（免按住拖拽，兼容 Win）」**（2026-08-14，已实现并推送 `4ad1a86`）。
> **v1.3 修改记录**：修正 **§9 双指平移方向**（2026-08-14，已实现并推送 `1cdbf9b`）。
> **v1.4 修改记录**：追加 **§10「平移边界橡皮筋（到边后仍可拖一小段）」**（2026-08-14，已实现并推送 `178194d`）。
> **v1.5 修改记录**：追加 **§11「切换时间周期固定最右侧 K 线时间」**（2026-08-14，已实现并推送 `178194d`）。
> **v1.6 修改记录**：追加 **§12「缩放时右侧价格贴框稳定（修复桶对齐破坏右边缘锚定）」**（2026-08-14，已实现并推送 `3b8a7e8`）。
> **v1.7 修改记录**：追加 **§13「模拟交易系统（开仓/杠杆/资金/挂单止盈止损）」**（2026-08-14 新增，分支 `feature/sim-trade`）。
> **v1.8 修改记录**：§13 评审修订（2026-08-14）：开仓价改最新 K 线价、爆仓简化、**止损也分批**、新增**加仓/减仓**、挂单数量改动态比例。
> **v1.9 修改记录**：追加 **§14「交易结果统计（资金走势 / 盈亏比分析）」**（2026-08-14 新增）。
> **v1.10 修改记录**：§13/§14 补充（2026-08-14）：**计算手续费**（开/平仓双向）；**历史买卖点在复盘可切换显示**。
> **v1.11 修改记录**：§13 新增 **交割单导出 CSV（Excel 兼容）**（2026-08-14 新增）。
> **v1.12 修改记录**：§13/§14 补充（2026-08-14）：**交易备注**、**移动止损 Trailing SL**、**按维度分组统计**、**平均持仓时间与交易频率**。
> **v1.13 修改记录**：追加 **§15「模拟交易测试用例」（S1–S16 / O1–O17 / M1–M17）**（2026-08-14 新增）。
> 原 §1–§7 数据同步内容保持不变。

## 1. 背景与目标

`btc-timeslicer`（BTC 时光机）为单文件复盘工具，数据内联于 `index.html`（`window.BTCFUT_DATA`），
当前截止 **2026-07-28**，已过期约半个月。

另有数据仓库 `github.com/sdlyingyong/k--data`（master 分支，**已更新至 2026-08-12**），
其 BTC 数据与 `btc-timeslicer` **完全同源**（币安 BTCUSDT 永续；已实测重叠 1370 根 15m K 线
O/C 零差异）。

目标：从 `k--data` 拉取最新 BTC 数据，同步重建 `btc-timeslicer` 的内联数据，
使页面数据更新至 2026-08-12（或 k--data 最新提交）。

## 2. 数据源与格式对照

| 项 | k--data（源） | btc-timeslicer（目标） |
| --- | --- | --- |
| 位置 | `btc_fut_data.js` + `fut_data/btc_15m_*.json` | `index.html` 内 `window.BTCFUT_DATA` |
| 时间格式 | 字符串 `"YYYY-MM-DD HH:MM:SS"`（UTC） | 数字：分钟 epoch（`t = ms/60000`） |
| 字段 | 9 元 `[t,o,h,l,c,v,quoteV,chg%,""]` | 6 元 `[t,o,h,l,c,v]` |
| 周期 | 15m 分块（chunkSize=20000）+ 1h/4h/1d 内联 + 5m | 1d/4h/1h/15m 全部内联 |
| 截止 | 2026-08-12 23:45（15m） | 2026-07-28 00:00 |

- 15m 根数：k--data 242905 vs 当前内联 241370（差 1535 ≈ 07-28→08-12 的 15m 根数，吻合）
- 成交量 v 均为币安 base 口径（BTC），无需换算
- 同源性已验证：重叠区间 1370 根 15m 逐根 O/C 完全一致

## 3. 同步方案

### 3.1 数据获取（二选一）
1. **本地模式**（推荐）：读取本地 `k--data` 仓库目录（存在则 `git pull`，不存在则 clone）
2. **远程模式**：经 `gh-proxy.com` 或直连 GitHub 拉取 `master` 分支的 `btc_fut_data.js` 与 13 个 15m 分块

### 3.2 转换与重建
1. 15m：分块按序拼接 → 时间戳转分钟 epoch → 截取 9 字段前 6 项
2. 1h/4h/1d：**从 15m 序列重采样**（O=时段首根 O，H/L 取极值，C=末根 C，V 求和）
   - 周期对齐：按 UTC 整界（60/240/1440 分钟）
3. 整体替换 `window.BTCFUT_DATA`，重建 `index.html`（其余代码零改动）
4. 保留文件体积：单文件 ~16MB 不变

### 3.3 校验（通过方可提交）
- 15m 时间连续：相邻间隔恒 = 15 分钟（允许 0 间隔=重复，报错）
- 各周期时间戳严格递增、无重复
- 重采样 1h/4h/1d 与 k--data 内联数组逐根核对（O/H/L/C/V 全等）
- 末根时间 = k--data 的 `last`（15m: 2026-08-12 23:45）
- 新旧数据重叠段（≤2026-07-28）15m 逐根一致（保证不篡改历史）

## 4. 已知差异（如实说明）

- **1d/4h/1h 的开盘价 O 将被修正**：现内联数据 O 采用「前收」惯例（O=前一根 C），
  k--data（币安真实数据）O 为时段内真实开盘价。重采样后历史 O 会变化（H/L/C/V 不变）。
  实测例：07-27 日线 O 65375.2 → 63662.7。**属数据修正而非回归**，PRD 记录此变更。
- 页面上线后数据仍会过期：本次同步为**一次性脚本同步**（与 k--data 更新节奏一致），
  实时在线更新（交易所直连）不在本期范围。

## 5. 交付物

| 文件 | 内容 |
| --- | --- |
| `sync_from_kdata.cjs` | 同步脚本（Node，无依赖）：拉取→转换→重采样→重建→校验 |
| `index.html` | 数据更新至 2026-08-12 |
| `README.md` | 数据来源与同步方式说明 |
| `PRD.md` | 本文档 |

## 6. 验收标准

1. 运行 `node sync_from_kdata.cjs`（本地模式）成功重建 `index.html`
2. 校验全通过（见 3.3），15m 末根 = 2026-08-12 23:45
3. 浏览器打开页面：四周期切换正常，最新 K 线显示 08-12，画线/记忆功能不受影响
4. `git commit + push` 后 GitHub Pages 线上数据同步至最新

## 7. 非目标（本期不做）

- 接入 k--data 的 5m/ETH/上证数据（btc-timeslicer 仅 BTC 15m 基底）
- 自动定时同步（GitHub Actions / cron）
- 浏览器端在线增量更新（此前 PRD 讨论过的 OKX 直连方案，本期不做）

---

# 附：§8 本次修改 — macOS 滚轮缩放灵敏度优化

版本：v1.1 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 8.1 背景与问题

用户反馈：**在 macOS 上滚轮缩放过于灵敏，不好用**。

### 8.1.1 现状代码（根因定位）

`index.html` 滚轮缩放处理（已按本 PRD 重写，见下方实现）：

```js
// 重写后：delta 累积 + 指数缩放 + rAF 节流
const ZOOM_SENSITIVITY = 1.15;
const DELTA_NORM = 120;
let wheelAccum = 0, wheelScheduled = false;
function applyZoom(delta) { /* factor = Math.pow(ZOOM_SENSITIVITY, delta); ... */ }
canvas.addEventListener('wheel', e => {
  e.preventDefault();
  wheelAccum += e.deltaY / DELTA_NORM;   // 归一化累积
  if (wheelScheduled) return;
  wheelScheduled = true;
  requestAnimationFrame(() => { wheelScheduled = false; ... applyZoom(wheelAccum); });
}, { passive: false });
```

修复前根因（已解决）：

| # | 根因 | 说明 |
|---|------|------|
| R1 | **固定百分比步进** | ~~每次滚轮事件缩放 15%（放大）或 13%（缩小），不随滚动幅度变化~~ → 改为幅度参与换算 |
| R2 | **未利用 `deltaY` 幅度** | ~~无论滚 1 格还是滚 10 格，缩放量完全一样~~ → 按 `deltaY/DELTA_NORM` 归一化后指数换算 |
| R3 | **无节流 / 无累积** | ~~每个 wheel 事件直接重算重绘~~ → rAF 帧内累积、帧末一次性生效 |
| R4 | 方向与系统滚动方向耦合 | 经分析现有语义（上推=放大、下滚=缩小）与主流图表惯例一致，**方向保持不变** |

### 8.1.2 影响

- 触控板用户轻扫即大幅缩放，无法停在目标区间
- 高频事件导致缩放抖动、难以回看特定 K 线区域
- 方向反直觉（取决于系统滚动方向设置）

## 8.2 目标

在**不改变现有功能**（画线、平移、记忆、各周期切换）的前提下：

1. 缩放**平滑、可精细控制**：滚动幅度与缩放量成正比
2. 触控板高频事件**累积后节流**，避免逐事件跳动
3. 方向**跟随系统滚动方向设置**（macOS「自然滚动」开/关均符合直觉）
4. 键盘步进（←/→）、拖动平移、多周期切换等现有交互**保持不受影响**

## 8.3 方案设计

### 8.3.1 核心改动（`index.html` wheel 处理器重写）

采用 **delta 累积 + 指数缩放 + rAF 节流** 的标准做法：

```
1. wheel 事件不直接缩放，而是把 deltaY 累积进 accumulator
2. 每帧（requestAnimationFrame）读取累积值，换算缩放步进：
   factor = Math.pow(ZOOM_SENSITIVITY, normalizedDelta)   // 连续、非固定步进
3. 换算后重置 accumulator，执行一次重绘（rAF 天然节流）
4. 方向：读系统设置判定是否「自然滚动」，决定 deltaY 符号是否取反
```

关键参数：

| 参数 | 默认 | 说明 |
|------|------|------|
| `ZOOM_SENSITIVITY` | 1.15 | 指数底数；一格标准滚动(120)的缩放倍数，与原始单事件手感基准一致；越小越"钝"（越不灵敏），可调 |
| `DELTA_NORM` | 120 | 单格标准 delta（鼠标一格=120），用于归一化触控板/鼠标 |
| 累积窗口 | rAF 帧 | 帧内累积，帧末一次性生效 |

### 8.3.2 缩放锚点保持

沿用现有逻辑：**缩放时右边缘（最新 K 线侧）固定不动**，只改变左侧可见历史量；
桶对齐（`viewStart` 量化到桶边界）保持不变，避免柱子跳动。

### 8.3.3 边界与钳制

沿用现有：`viewCount` 钳制在 `[20, len]`，`viewStart` 钳制在 `[0, len-viewCount]`。

### 8.3.4 兼容性

- `preventDefault` 保留（阻止页面滚动/缩放冲突）
- `passive: false` 保留（必须，否则 preventDefault 无效）
- 鼠标滚轮（每格 120）与触控板（小 delta 高频）经归一化后表现一致

## 8.4 交互细节

- **触控板**：轻滚 → 小幅度连续缩放；快速长滚 → 快速缩放（按幅度自然放大）
- **鼠标滚轮**：一格 = 标准幅度缩放（与原来接近，但连续而非跳变）
- **方向**：macOS「自然滚动」开启时，双指上推（deltaY 为负）应**放大**（符合用户预期）
- 不改动：画线工具、拖动平移、←/→ 步进、G 跳转、Del 删除、盈亏比拖拽

## 8.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| A1 | 触控板轻滚一下 | 缩放幅度小且连续，可停在目标区间 |
| A2 | 触控板快速长滚 | 缩放加速但平滑，无逐事件跳变 |
| A3 | 鼠标滚轮一格 | 缩放一档，方向与幅度合理 |
| A4 | 系统「自然滚动」开/关各测一次 | 方向均符合直觉（上推放大/上推缩小随设置） |
| A5 | 缩放后右边缘最新 K 线 | 保持固定不漂移 |
| A6 | 缩放范围 | 最小/最大钳制不越界，不白屏不报错 |
| A7 | 缩放后画线/平移/记忆 | 现有功能全部正常 |
| A8 | 性能 | 高频滚动下帧率稳定，无卡顿（rAF 节流生效） |

## 8.6 非目标（本次修改不做）

- 不改动画线、平移、记忆、周期切换等其他交互
- 不做双击缩放 / 按钮缩放 / 快捷键缩放（可后续）
- 不做 Windows 专项（此改动对 Windows 同样受益，但以 mac 为主要验证环境）
- 不做缩放平滑动画插值（保持即时响应）

## 8.7 测试策略

1. **手动测试**（主）：macOS 触控板 + 鼠标各跑一遍 §8.5 验收清单
2. **回归**：现有 `tests/regression.test.cjs` 必须全过（保证画线/数据/记忆无回归）
3. **参数可调**：`ZOOM_SENSITIVITY` 提取为常量，便于后续按反馈调参

## 8.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | 重写 wheel 处理器（累积 + 指数缩放 + rAF 节流；方向保持） |
| `tests/regression.test.cjs` | 新增 wheel 缩放回归断言（方向/幅度/锚定/钳制，共 9 项） |
| `PRD.md` | 本文档（v1.1，含本修改章节） |

## 8.9 风险与对策

| 风险 | 对策 |
|------|------|
| rAF 累积导致缩放延迟（手感"肉"） | 帧间隔极短（16ms），感知可忽略；若需更跟手可降为「事件触发 + 节流」混合 |
| 系统滚动方向 API 兼容性 | 经分析现有方向语义符合图表惯例，方向不变（无需依赖系统 API） |
| 灵敏度参数不适配所有用户 | 参数常量化（`ZOOM_SENSITIVITY`）+ 文档说明，后续可加 UI 滑块 |
| 回归风险 | 只改 wheel 处理器一个函数；跑全量 regression 测试 |

## 8.10 完成定义

- [x] 自动化测试：`tests/regression.test.cjs` **64/64 通过**（基线 55 + wheel 新增 9），覆盖方向/幅度/锚定/钳制
- [ ] §8.5 手动验收 A1–A8（macOS 触控板/鼠标实机手感）
- [x] 代码修改已落地（wheel 处理器重写）
- [ ] 提交并推送，GitHub Pages 线上验证缩放手感

---

# 附：§9 本次修改 — 触控板双指滑动平移（免按住拖拽，兼容 Win）

版本：v1.2 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 9.1 背景与需求

当前平移 K 线需要**按住鼠标左键拖拽**（canvas mousedown + mousemove）。用户反馈：

> mac 上希望**不按下鼠标键**、直接用**触控板双指滑动**拖动平移，同时要兼容 Windows。

macOS 与 Windows 触控板的双指滑动都会产生浏览器 `wheel` 事件（带 `deltaX`/`deltaY` 分量），
因此**双指平移 = 复用 wheel 事件的 `deltaX` 横向分量**，天然跨平台兼容，无需平台判断。

### 9.1.1 现状

- `index.html` wheel 处理器（§8 重写后）：只处理 `deltaY`（缩放），**`deltaX` 被忽略**
- 平移仅支持：按住左键拖拽（`viewStart -= dx / xW`，xW = plotW / viewCount）

### 9.1.2 影响

- mac 触控板用户无法用习惯的双指手势平移（主流图表工具均支持）
- Windows 触控板双指滑动同样无效

## 9.2 目标

1. **双指横向滑动（wheel deltaX）→ 平移视图**，无需按住鼠标键
2. **兼容 Windows**：同一套 wheel 事件实现，无需平台分支
3. 双指纵向滑动（deltaY）→ 继续缩放（§8 行为不变）
4. 原按住拖拽平移、画线工具、缩放等全部保持
5. 平移手感与拖拽一致（像素→K线换算同 `dx / xW`）

## 9.3 方案设计

### 9.3.1 核心改动（wheel 处理器扩展）

在 §8 的 wheel 处理器中增加 `deltaX` 平移分支：

```
1. wheel 事件拆分：
   - deltaX ≠ 0 → 平移：viewStart -= (deltaX / DELTA_NORM_PX) * panPerStep
   - deltaY ≠ 0 → 缩放（沿用 §8 累积+指数+rAF）
2. 平移换算与拖拽一致：ΔviewStart = -Δpx / xW（xW = plotW / viewCount）
3. 边界钳制：viewStart ∈ [0, len - viewCount]（与拖拽一致）
4. 平移也用 rAF 节流 + 累积（与缩放同框架，避免触控板高频事件抖动）
```

关键换算：

| 项 | 取值 | 说明 |
|---|---|---|
| 平移像素 | `deltaX` 累积 | 触控板双指滑动产生像素级 deltaX |
| K线换算 | `ΔviewStart = -Δpx / xW` | 与拖拽平移（第 1900 行）同一公式，手感一致 |
| 钳制 | `[0, len - viewCount]` | 与拖拽一致 |
| 节流 | rAF 帧内累积 | 与 §8 缩放同框架 |

### 9.3.2 方向语义（v1.3 修正）

> **v1.3 修正**：v1.2 实现方向与用户实测相反，已翻转。

- 双指向**左**滑（deltaX < 0，自然滚动）→ 图表内容左移 → **看更早（历史）** → **viewStart 减小**
- 双指向**右**滑（deltaX > 0）→ 图表内容右移 → **看更近（最新）** → **viewStart 增大**
- 换算公式：`viewStart += px / xW`（v1.3 从 `-=` 翻转为 `+=`）
- 不依赖平台：mac/win 触控板均按浏览器实际 deltaX 方向直接换算

### 9.3.3 兼容性

- 鼠标滚轮：传统滚轮无 deltaX（除非水平滚轮），行为不变（仍只缩放）
- 缩放与平移互不干扰：deltaX/deltaY 分别累积，同一帧内各自生效
- `preventDefault` 保留（阻止页面自身滚动/缩放冲突）

## 9.4 交互细节

- mac 触控板：双指左右滑 = 平移；双指上下滑 = 缩放；单指/鼠标 = 原有行为
- Windows 触控板：双指左右滑 = 平移（同样生效）
- 按住左键拖拽平移：继续可用（不变）
- 平移过程中最新 K 线不再"钉住"（与拖拽一致，可自由看历史）

## 9.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| B1 | mac 双指左右滑动 | **左滑=看更早（历史）、右滑=看更近（最新）**，无需按鼠标（v1.3 方向已翻转） |
| B2 | mac 双指上下滑动 | 仍为缩放（§8 行为不变） |
| B3 | Windows 触控板双指左右滑 | 平移生效（兼容 win） |
| B4 | 鼠标滚轮 | 仍只缩放，不触发平移 |
| B5 | 按住左键拖拽 | 平移仍正常（不回归） |
| B6 | 平移边界 | viewStart 不越界，不白屏 |
| B7 | 平移+缩放同帧 | 两个手势同时生效不冲突 |
| B8 | 画线/时间轴/记忆 | 现有功能全部正常 |

## 9.6 非目标（本次修改不做）

- 不做惯性滑动/动量缓动（保持即时响应）
- 不做触控屏（touch）手势（纯触控板/鼠标范围）
- 不改动缩放灵敏度参数（§8 已定）

## 9.7 测试策略

1. **自动化**：`tests/regression.test.cjs` 增加 deltaX 平移断言：
   - deltaX 触发 viewStart 变化（方向正确）
   - 平移换算 ≈ 拖拽换算（同公式验证）
   - 边界钳制不越界
   - deltaY 缩放不回归（已有断言）
2. **手动**（mac/win 实测）：§9.5 B1–B8

## 9.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | wheel 处理器增加 deltaX 平移分支（累积 + rAF + 拖拽同换算） |
| `tests/regression.test.cjs` | 新增 deltaX 平移断言（v1.3 更新方向期望） |
| `PRD.md` | 本文档（v1.3，含本修改章节） |

## 9.9 风险与对策

| 风险 | 对策 |
|------|------|
| deltaX 平移与缩放同时触发误判 | deltaX/deltaY 分量分别累积，独立生效 |
| 部分浏览器/系统不产生 deltaX | 无 deltaX 时平移分支自然不触发，不影响缩放 |
| ~~方向与用户系统设置不一致~~ | **v1.3 已修正**：实测方向反了，换算改为 `viewStart += px / xW`（左滑=看更早） |
| 回归风险 | 只扩展 wheel 处理器；跑全量 regression |

## 9.10 完成定义

- [x] 自动化测试：`tests/regression.test.cjs` 全过（v1.3 方向断言已翻转：左滑=viewStart 减小）
- [ ] §9.5 手动验收 B1–B8（mac + win 实测）
- [x] 代码修改已落地（wheel 处理器 deltaX 平移分支）
- [ ] 提交并推送，GitHub Pages 线上验证双指平移

---

# 附：§10 本次修改 — 平移边界橡皮筋（到边后仍可拖一小段）

版本：v1.4 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 10.1 背景与需求

用户反馈：双指滑动平移**到左边界（历史尽头）后就完全拖不动了**，
希望像主流图表那样**到边后还能继续拖一小段**（露出边界外空白），松手回弹。

### 10.1.1 现状

`applyPan` 中 `viewStart` 被硬钳制在 `[0, len - viewCount]`：
```js
viewStart = Math.max(0, Math.min(len - viewCount, viewStart + px / xW));
```
一旦 `viewStart` 到达 0（左）或 `len - viewCount`（右），后续平移完全无效，
手势"卡死"，无任何视觉反馈。

### 10.1.2 影响

- 触控板用户到边界时手势无响应，体验生硬
- 主流图表（TradingView 等）均有橡皮筋/回弹反馈，属基本交互预期

## 10.2 目标

1. 到边界后**仍可继续拖一小段**（橡皮筋），有视觉反馈（露出边界外空白）
2. 停止拖动后**自动回弹**到边界
3. 不改变 `viewStart` 合法范围（避免破坏 draw/hitTest/分块加载等依赖）
4. 缩放、画线、其他交互全部不受影响

## 10.3 方案设计

采用**像素级显示偏移（panRubber）**，不动 `viewStart`：

```
1. 新增全局 panRubber（像素），范围 [-MAX, +MAX]（默认 ±90px）
2. applyPan：
   - 先抵消反向橡皮筋（往回拖时先"吃掉"panRubber）
   - 剩余量移动 viewStart（仍 clamp 合法范围）
   - 被 clamp 吃掉的量累积进 panRubber（限幅）
3. 绘制：draw() 开头 ctx.save()+ctx.translate(panRubber,0)，结尾 restore()
   —— 整个画布随橡皮筋偏移，露出边界空白，无需改各绘制坐标
4. 回弹：wheel 停止 ≥120ms 后，rAF 逐帧衰减 panRubber 至 0（指数衰减）
```

| 项 | 取值 | 说明 |
|---|---|---|
| `panRubber` | 像素 | 显示偏移，不动 viewStart |
| 限幅 `PAN_RUBBER_MAX` | ±90px | 防止拖太远 |
| 回弹触发 | 距上次 wheel ≥120ms | 帧循环衰减 |
| 衰减系数 | 0.85/帧 | 约 10 帧归零（平滑） |

## 10.4 交互细节

- 左边界继续左滑 → 内容向右露出左侧空白（橡皮筋拉出）
- 松手 → 平滑回弹到边界
- 往回拖 → 先抵消橡皮筋，再正常移动视图
- 不影响：缩放、画线、hitTest（橡皮筋仅短暂显示态，命中偏差可接受）

## 10.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| C1 | 左边界继续左滑 | viewStart 仍 =0，但画面向右偏移（橡皮筋生效） |
| C2 | 右边界继续右滑 | 同理向左偏移 |
| C3 | 松手后 | 画面平滑回弹到边界 |
| C4 | 往回拖 | 先抵消橡皮筋再移动视图 |
| C5 | 橡皮筋限幅 | 不超过 ±90px |
| C6 | 缩放/画线 | 不受影响（回归） |

## 10.6 非目标

- 不做触控/惯性滑动
- 不引入 CSS/布局层偏移（canvas 内 translate 足够）

## 10.7 测试策略

1. **自动化**：`regression.test.cjs` 暴露 `getPanRubber`：
   - 边界后继续滑 → panRubber 非零、viewStart 不变
   - 限幅 ≤90
   - 往回拖抵消橡皮筋
   - 原平移/缩放断言全过（回归）
2. **手动**：mac 触控板实机验证回弹手感

## 10.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | applyPan 橡皮筋 + draw translate + 回弹帧循环 |
| `tests/regression.test.cjs` | 橡皮筋断言 |
| `PRD.md` | 本文档（v1.4） |

## 10.9 完成定义

- [x] 自动化测试全过：橡皮筋 6 项断言（边界生效/限幅 90px/往回拖抵消/右边界对称/恢复）
- [ ] 手动验收 C1–C6（mac 实测回弹手感）

---

# 附：§11 本次修改 — 切换时间周期固定最右侧 K 线时间

版本：v1.5 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 11.1 背景与需求

用户反馈：**切换时间周期（1d/4h/1h/15m）时，希望最右侧那根 K 线的时间保持不变**
（锚定当前视角的时间点），而不是跳回最新或偏位。

### 11.1.1 现状与根因

代码已有"右侧日期对齐"逻辑（`setView` 捕获 `rightTs` 并在新周期 `findIdxByTime` 对齐），
但存在两个缺陷导致实际不生效/不准：

| # | 缺陷 | 位置 |
|---|------|------|
| R1 | **最右索引偏 1 根**：`rIdx = viewStart + viewCount` 比真正最右可见根（`e-1`）多 1 | `setView` rightTs 捕获处 |
| R2 | **viewCount 被无条件重置为 260**：切换周期后缩放级别丢失，右侧时间虽对齐但视觉跳变；若 `findIdxByTime` 找不到（数据未加载）则直接跳到最新 | `setView` 开头 `viewCount = min(260, len)` |

### 11.1.2 影响

- 切周期后最右侧 K 线时间偏移 1 根，或直接跳回最新，用户无法保持"在同一时间点看不同周期"

## 11.2 目标

1. 切换周期时**最右侧 K 线时间保持不变**（锚定 rightTs）
2. **保持缩放级别**：切换周期后 `viewCount` 不变（用户放大多大就多大）
3. 首次启动/切标的逻辑不受影响

## 11.3 方案设计

```
1. rightTs 捕获修正：rIdx = max(0, floor(viewStart + viewCount) - 1)
   —— 取真正最右可见 K 线（draw 的可见区 e-1）
2. viewCount 保持：周期切换分支不再重置为 260；
   仅首次启动（viewCount==0）才设默认 260
3. 对齐：viewStart = clamp(ni - viewCount, 0, len - viewCount)
   —— 右侧对齐到 rightTs 在新周期中的索引 ni
```

## 11.4 交互细节

- 在 1d 看 2024-03-01 附近 → 切 4h → 最右侧仍显示 2024-03-01 附近那根 4h K 线
- 停在最新 → 切周期 → 仍停在最新（rightTs=最新时间）
- 缩放到 50 根 → 切周期 → 仍是 50 根（缩放级别保留）

## 11.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| D1 | 中间位置切周期 | 新周期最右侧时间 == 原周期最右侧时间（±1 根） |
| D2 | 最新位置切周期 | 仍停在最新 |
| D3 | 放大后切周期 | viewCount 保持（不跳回 260） |
| D4 | 首次启动 | 默认 260 根（不变） |
| D5 | 切标的 | 恢复该标的记忆位置（不变） |

## 11.6 非目标

- 不改动画线/记忆/缩放等其他逻辑
- 不做周期切换的平滑过渡动画

## 11.7 测试策略

1. **自动化**：`regression.test.cjs` 暴露 `setView`：
   - 当前 1d 视图最右时间 → `await setView(null,'4h')` → 新最右时间 == 原（±1 根）
   - viewCount 在切换前后一致
   - 切回 1d 仍对齐
2. **手动**：mac 实机各周期切换验证 D1–D5

## 11.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | rightTs 索引修正 + viewCount 保持 |
| `tests/regression.test.cjs` | 周期切换锚定断言 |
| `PRD.md` | 本文档（v1.5） |

## 11.9 风险与对策

| 风险 | 对策 |
|------|------|
| `findIdxByTime` 在分块数据未加载时失败 | 二分前 ensure 首尾；失败回退到最新（现有行为） |
| viewCount 保持破坏首次启动 | 仅 viewCount==0 时设默认，启动路径不受影响 |
| 回归风险 | 只改 setView 内两处；跑全量 regression |

## 11.10 完成定义

- [x] 自动化测试全过：周期锚定 5 项断言（viewCount 保持 / 跨周期最右时间 Δ0min 精确锚定 / 切回一致）
- [ ] 手动验收 D1–D5（mac 实测）
- [ ] 提交并推送，GitHub Pages 线上验证

---

# 附：§12 本次修改 — 缩放时右侧价格贴框稳定（修复右边缘锚定）

版本：v1.6 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 12.1 背景与需求

用户反馈：**右侧价格（最右可见 K 线）应该一直紧贴右侧边框**，但缩放时会跳来跳去。

### 12.1.1 现状与根因

`applyZoom` 中：
```js
const rightEdge = viewStart + viewCount;   // 缩放前右边缘锚点
viewCount = nc;
viewStart = clamp(rightEdge - viewCount, 0, len - viewCount);  // ← 右边缘锚定
const bkt = Math.max(1, Math.round(viewCount / plotW));
viewStart = Math.round(Math.floor(viewStart / bkt) * bkt);     // ← 桶对齐量化，破坏锚定！
```

| # | 根因 | 说明 |
|---|------|------|
| R1 | **桶对齐量化破坏右边缘锚定** | `viewStart` 量化到桶边界（bkt 倍数）后，右边缘 = 量化值 + viewCount ≠ rightEdge。当 `viewCount > plotW`（≈1184，缩小看很多根）时 bkt > 1，最右 K 线左右跳 ±bkt 根，价格随之跳动 |

### 12.1.2 影响

- 缩小视图（15m 全量等）缩放时，最右侧 K 线不贴右框，左右跳
- 用户期望：**右侧价格/最右 K 线一直紧贴右框**

## 12.2 目标

1. 缩放时**右边缘严格锚定**（最右 K 线贴右框，偏差 0 根）
2. 尽可能保留桶对齐的柱子稳定性（仅在两者冲突时优先锚定）

## 12.3 方案设计

```
1. viewStart 先按右边缘锚定（现状）
2. 桶对齐量化后，检查是否严格保持右边缘：
   - 量化后右边缘不变（偏差 0）→ 采用量化值（保留柱子稳定）
   - 否则 → 放弃量化，保持右边缘锚定（右侧贴框优先）
```

| 项 | 策略 |
|---|---|
| 量化检查 | `|量化viewStart + viewCount - rightEdge| <= 1e-6` 才采用量化值 |
| 冲突时 | 保持右边缘锚定（用户需求优先，偏差 0） |
| bkt=1（常见） | viewStart 为整数时量化无影响，锚定天然成立 |
| 副作用 | 大缩放（bkt>1）时量化常被跳过，柱子可能有细微走动（右侧贴框优先，可接受） |

## 12.4 交互细节

- 缩放任何级别：最右 K 线始终贴右框（右边缘不变）
- 大缩放（bkt>1）时柱子稳定性让位于右侧锚定（可接受）

## 12.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| E1 | 小缩放（bkt=1）缩放 | 右边缘精确不变（偏差 0） |
| E2 | 大缩放（bkt>1）缩放 | 右边缘偏差 ≤ 1 根（尽量 0） |
| E3 | 缩放到边界 | 不越界，不白屏 |
| E4 | 缩放+平移组合 | 右边缘逻辑不冲突 |

## 12.6 非目标

- 不改动画线/时间轴/成交量
- 不做价格轴刻度平滑（刻度随 pmin/pmax 变化属标准行为）

## 12.7 测试策略

1. **自动化**：`regression.test.cjs` 新增严格右边缘断言：
   - 缩小到 viewCount > plotW（bkt>1）→ 缩放后 `viewStart + viewCount == 缩放前 rightEdge`（偏差 0）
   - bkt=1 场景同样偏差 0
   - 边界钳制回归
2. **手动**：mac 实测缩小 15m 缩放时右侧贴框稳定

## 12.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | applyZoom 量化保护（锚定优先） |
| `tests/regression.test.cjs` | 严格右边缘锚定断言 |
| `PRD.md` | 本文档（v1.6） |

## 12.9 风险与对策

| 风险 | 对策 |
|------|------|
| 放弃量化导致大缩放柱子轻微漂移 | 仅当量化破坏右边缘（偏差>0）时放弃；右侧贴框为用户明确诉求，优先 |
| 回归风险 | 只改 applyZoom 两行；跑全量 regression |

## 12.10 完成定义

- [x] 自动化测试全过：严格右边缘锚定断言（小缩放≤0.5 根 / 大缩放区间连续放大恒锚定 / 状态合法）
- [ ] 手动验收 E1–E4（mac 实测右侧贴框稳定）
- [ ] 提交并推送，GitHub Pages 线上验证

---

# 附：§13 本次修改 — 模拟交易系统（开仓/杠杆/资金/挂单止盈止损）

版本：v1.7 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 13.1 背景与目标

在 btc-timeslicer（离线单文件复盘工具）中增加**模拟交易**能力：
用**虚拟资金**在历史 K 线上演练开仓/杠杆/止盈止损，检验交易纪律（如"每次只用 1/3 资金"）。

### 13.1.1 用户设想（需求来源）

- 可以**开仓**，选择 **5x / 10x 杠杆**
- 资金可**划转到钱包**（钱包与交易账户分离；每次开仓只用账户 1/3，多了转走——纯手动纪律）
- 开仓后设置**自动止损**，价格到了自动平仓
- **止盈分批卖出**：在图上**拖动仓位线**到目标价位生成止盈挂单，设定**平仓数量**，可挂多个分批止盈

### 13.1.2 关键决策（已与用户确认）

| 决策点 | 选择 |
|---|---|
| 价格来源 | **历史 K 线推进**：开仓基于当前视图最新 K 线价；切到更晚 K 线时检测挂单触发（离线可用） |
| 开仓价 | **当前视图最新一根 K 线的收盘价**（非点选 K 线） |
| 杠杆模型 | **真实合约**：保证金 = 开仓金额；仓位价值 = 保证金 × 杠杆 |
| 止损行为 | **触发即平仓结算**（按该档剩余仓位）；**止损也支持分批**（多档 SL） |
| 止盈 | **分批卖出**：多档挂单，每档触发卖对应数量，剩余继续持有 |
| 止盈/止损交互 | **拖动仓位线**生成挂单（价位 + 平仓数量，数量为**相对当前持仓的动态比例**） |
| 加仓/减仓 | **支持中间加仓/减仓**（成本价加权平均；挂单比例按新持仓量生效） |
| 爆仓 | **简化处理**（止损保护下极少触发；亏损 ≥ 保证金即全平归零，不做精确强平价） |
| 1/3 纪律 | 纯手动划转，系统不强制 |
| 方向 | 多空都支持 |
| 平仓 | 自动止损 + 手动平仓都要 |

## 13.2 资金模型

### 13.2.1 账户结构

| 项 | 说明 |
|---|---|
| 总资产 | 钱包余额 + 交易账户余额 + 持仓浮盈（显示用） |
| 钱包（Wallet） | 存放闲钱，不参与交易 |
| 交易账户（Account） | 可用保证金 + 已占用保证金（持仓） |
| 划转 | 钱包 ↔ 账户 双向手动划转（数量自填） |

初始资金：**10,000 USDT（模拟）**，存入钱包。

### 13.2.2 杠杆与保证金（真实合约）

| 项 | 公式 |
|---|---|
| 保证金 | 开仓时投入的 USDT（≤ 账户可用） |
| 仓位价值 | 保证金 × 杠杆（5x/10x） |
| 开仓数量 | 仓位价值 ÷ 开仓价（BTC 数量） |
| 盈亏 | (平仓价 − 开仓价) / 开仓价 × 仓位价值（多头）；空头反向 |
| 爆仓 | **简化处理**：止损保护下极少触发；仅当亏损 ≥ 保证金时自动全平、该笔保证金归零（不做精确强平价） |

### 13.2.3 手续费（v1.10 新增）

| 项 | 公式 / 说明 |
|---|---|
| 费率 | 默认 **0.05%**（可配置，模拟 taker），开仓/平仓双向收取 |
| 手续费基数 | **仓位价值（名义价值）= 保证金 × 杠杆**（真实合约口径） |
| 开仓手续费 | 开仓仓位价值 × 费率（含加仓：按加仓金额 × 杠杆 × 费率） |
| 平仓手续费 | 平仓金额（卖出部分的名义价值）× 费率（含减仓/挂单成交/手动平仓） |
| 结算口径 | **已实现盈亏 = 价格盈亏 − 开仓手续费 − 平仓手续费**；资金曲线/统计均按扣费后口径 |

> 例：1000U 保证金 × 5x = 5000U 名义价值，开仓手续费 5000 × 0.05% = 2.5U；平仓同理再扣一次。

## 13.3 开仓

1. **开仓价 = 当前视图最新一根 K 线的收盘价**（无需点选 K 线）
2. 交易面板填写：**方向**（多/空）、**杠杆**（5x/10x）、**保证金**（USDT，≤ 账户可用）
3. 确认开仓 → 生成持仓，图表显示**仓位线**（开仓价水平线，带方向/杠杆/数量标识）
4. 开仓后：账户可用 -= 保证金，已占用 += 保证金

### 13.3.1 加仓 / 减仓（v1.8 新增）

- **加仓**：持仓过程中追加保证金 → 仓位数量增加，**成本价按加权平均**更新，已占用 += 追加保证金
- **减仓**：持仓过程中卖出一部分（按当前最新 K 线价）→ 仓位数量减少，**该部分盈亏即时结算**进账户，已占用按比例减少
- 加仓/减仓后：所有挂单的**数量比例基于最新持仓量**动态生效（比例不变，对应数量随持仓量变化）
- 加仓/减仓后仓位线（成本价）随之更新

## 13.4 挂单式止盈止损（拖动仓位线）

### 13.4.1 交互

- 持仓在图表上显示为一条**可拖动的仓位线**（开仓/成本价）
- **拖动**该线到目标价位松开 → 弹出该挂单的**平仓数量设置**（比例 %，默认按剩余仓位的 1/3）
- 方向判定：
  - **多头**：往上拖 = 止盈挂单（TP）；往下拖 = 止损挂单（SL）
  - **空头**：往下拖 = 止盈挂单（TP）；往上拖 = 止损挂单（SL）
- 一个持仓可挂**多个止盈单**（分批，如 TP1/TP2/TP3）**和多个止损单**（分批，如 SL1/SL2）（v1.8：止损也支持分批）
- 挂单在图上显示为带价位标签的横线（TP 绿 / SL 红），可再次拖动调整、删除

### 13.4.2 挂单数据

| 字段 | 说明 |
|---|---|
| 类型 | TP（止盈）/ SL（止损） |
| 价位 | 拖动到的价格 |
| 数量 | **相对当前持仓数量的比例 %**（动态：加仓/减仓后按新持仓量计算实际卖出数量；v1.8 确认） |
| 状态 | 挂单中 / 已触发 / 已取消 |

### 13.4.3 触发与成交

- 用户把视图推进到更晚的 K 线（←→ 步进 / 时间轴点击 / 双指平移 / 切周期）
- 系统检查该段 K 线（上次检查点到当前点）是否**穿越**挂单价（K 线 high/low 触及即成交）
- 成交：按挂单数量卖出对应仓位，盈亏结算进账户（已占用 -= 该部分保证金），**扣减平仓手续费（§13.2.3）**
- **止损触发（分批 SL）→ 只卖该档数量**，剩余继续持有（v1.8：止损可分批，同止盈逻辑）
- **某档止盈触发 → 只卖该档数量，剩余继续持有**（等下一档或止损）
- 同段 K 线内多档挂单均触发 → 按顺序逐档成交（止盈优先于同价位止损）
- 全部仓位卖出 → 持仓关闭，记录历史

### 13.4.4 手动平仓 / 减仓

- 持仓列表提供"平仓"按钮 → 按当前视图最新 K 线价全部卖出，立即结算
- 持仓列表提供"减仓"输入 → 按当前价卖出一部分（见 §13.3.1）

### 13.4.5 移动止损 Trailing SL（v1.12 新增）

- 止损挂单可勾选**移动止损（Trailing）**模式，参数：**跟踪距离**（固定 USDT 或 %）
- 跟踪逻辑（每推进一根 K 线检查一次）：
  - **多头**：`新SL = max(当前SL, 区间最高价 − 跟踪距离)`（止损只上移不回落）
  - **空头**：`新SL = min(当前SL, 区间最低价 + 跟踪距离)`（只下移）
- **保本移动**（可选开关）：盈利超过开仓价后，SL 至少移到开仓价（锁定不亏）
- 图上显示移动后的 SL 位置（线随推进更新），成交判定同普通 SL
- 与分批止盈配合：TP1 成交后剩余仓位可配 Trailing SL 锁定利润

## 13.5 持仓与历史

| 面板 | 内容 |
|---|---|
| 交易面板 | 钱包余额 / 账户余额 / 可用 / 已占用 / 总资产 |
| 持仓列表 | 每笔：方向 / 杠杆 / 成本价 / 数量 / 当前价 / 浮盈 / 挂单列表 |
| 持仓操作 | 加仓（追加保证金）/ 减仓（卖出一部分）/ 平仓（全部） |
| 挂单操作 | 新建（拖动）/ 调整（拖动）/ 删除 |
| 历史记录 | 已平仓：开平仓价 / 数量 / **手续费** / 盈亏（扣费后）/ 触发方式（止损/止盈/手动/减仓）/ 买卖点 |

数据持久化：localStorage（`sim_trade_v1`），重启保留。

### 13.5.1 历史买卖点（v1.10 新增）

- 每笔已平仓持仓记录其**买卖点**：开仓点（时间, 开仓价）+ 每个平仓点（时间, 平仓价, 触发方式）
- 复盘时可在图上**切换显示历史买卖点**（标记开仓 ▲/平仓 ●，颜色区分多空与盈亏）
- 切换入口在统计/复盘面板（§14），可显示全部或按某笔查看

### 13.5.2 交割单导出（v1.11 新增）

- **交割单** = 全部成交流水（每笔开仓/加仓/减仓/平仓各一行），按时间排序
- 每行字段：`序号 / 时间 / 类型（开仓/加仓/减仓/平仓-TP/平仓-SL/手动平仓）/ 方向 / 杠杆 / 价格 / 数量(BTC) / 名义价值 / 保证金 / 手续费 / 盈亏 / 累计权益 / 备注`
- **导出格式**：**CSV（UTF-8 BOM，Excel 直接打开中文不乱码）**——零依赖，保持单文件离线特性
- 导出入口：统计/复盘面板"导出交割单"按钮，下载 `交割单_YYYY-MM-DD.csv`
- 数据来源：历史记录（§13.5）+ 成交流水；含手续费（§13.2.3）
- （后续可选）若需 .xlsx 原生格式，再内联极简 xlsx 写入器

### 13.5.3 交易备注 / 日志（v1.12 新增）

- 每笔持仓（进行中或已平仓）可添加**备注**（自由文本，如开仓理由、复盘反思）
- 备注在持仓列表 / 历史记录中显示，可随时编辑
- 交割单导出（§13.5.2）的"备注"列 = 该笔持仓的备注
- 用途：复盘时回看"当时为什么这么操作"，修正交易决策

## 13.6 交互细节

- 仓位线/挂单线复用现有画线系统（hline 风格），可命中、可拖动
- 拖动时显示实时价位 + 数量提示
- 推进时间触发成交后，弹出 toast/面板提示（如 "TP1 @ 66500 成交，卖出 1/3"）
- 全部挂单与持仓在切周期后保持（按时间锚定）

## 13.7 验收标准

| # | 用例 | 预期 |
|---|------|------|
| T1 | 开仓（多/空，5x/10x，最新 K 线价） | 账户扣保证金，图表出现仓位线 |
| T2 | 钱包↔账户划转 | 余额正确增减 |
| T3 | 拖动仓位线生成 TP/SL 挂单 | 方向正确（多：上TP下SL）；可设数量比例 |
| T4 | 多档止盈分批 | 每档触发只卖对应数量，剩余持有 |
| T5 | 止损分批 | 每档 SL 触发只卖该档数量，剩余继续持有（v1.8） |
| T6 | 推进时间触发成交 | 价格穿越挂单价即成交，提示正确 |
| T7 | 手动平仓 | 按当前价全平结算 |
| T8 | 加仓 | 数量增加、成本价加权平均、挂单比例按新持仓量生效 |
| T9 | 减仓 | 卖出部分即时结算、数量减少、剩余持仓与挂单正确 |
| T10 | 爆仓 | 亏损 ≥ 保证金时自动平仓（简化） |
| T11 | 持久化 | 刷新后持仓/挂单/余额保留 |
| T12 | 回归 | 画线/缩放/平移/切周期不受影响 |
| T13 | **移动止损** | 价格有利移动时 SL 跟随上移/下移，回落触发平仓（构造推进序列验证） |
| T14 | **交易备注** | 持仓/历史可添加编辑备注，交割单导出含备注列 |

## 13.8 非目标（本期不做）

- 不接真实行情/真实下单（纯模拟）
- 不做强平价精确计算（简单爆仓处理）
- 不做 1/3 自动纪律（纯手动）
- 不做多账户/多币种

## 13.9 测试策略

1. **单元**：盈亏计算、杠杆换算、加权平均成本（加仓）、挂单触发判定（K 线穿越）、分批结算（TP/SL 多档）
2. **交互**：拖动仓位线生成挂单、数量设置、加仓/减仓
3. **推进集成**：模拟推进多根 K 线，验证 TP/SL 触发顺序、分批逻辑、加仓后挂单动态比例
4. **回归**：现有 87 项全过

## 13.10 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | 模拟交易模块（资金/开仓/加仓减仓/挂单/结算/面板） |
| `tests/regression.test.cjs` | 交易逻辑测试 |
| `PRD.md` | 本文档（v1.8） |

## 13.11 风险与对策

| 风险 | 对策 |
|------|------|
| 拖动挂单与现有画线工具冲突 | 交易模式下独占拖动；挂单线独立于画线存储 |
| 推进触发判定复杂度 | 集中一个"检查 K 线段触发"函数，遍历挂单判定 |
| 浮点金额精度 | 金额用整数分（USDT×100）存储，显示时格式化 |
| 回归风险 | 独立模块化，跑全量 regression |

## 13.12 完成定义

- [ ] 自动化测试全过（含交易逻辑测试）
- [ ] 手动验收 T1–T10（mac 实测）
- [ ] 提交 feature/sim-trade 分支，评审后合入 main

---

# 附：§14 本次修改 — 交易结果统计（资金走势 / 盈亏比分析）

版本：v1.9 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

## 14.1 背景与需求

模拟交易（§13）跑起来后，用户需要一个**查看结果的地方**：

> 我就是模拟整个合约交易的结果，然后最好有个看结果的地方，
> 能够看**资金走势**，然后**盈亏比分析**。

目标：一个**交易统计面板**，汇总已平仓交易的结果，可视化资金曲线与盈亏质量。

## 14.2 统计面板（看结果的地方）

### 14.2.1 概览指标

| 指标 | 定义 |
|---|---|
| 总资产 | 钱包 + 账户 + 持仓浮盈（当前） |
| 已实现盈亏 | 所有已平仓/减仓结算的盈亏总和（**已扣手续费**） |
| 未实现盈亏 | 当前持仓浮盈（按最新 K 线价，未扣平仓手续费） |
| 交易次数 | 已平仓笔数（按"完整持仓生命周期"计笔） |
| 胜率 | 盈利平仓笔数 ÷ 总平仓笔数（**扣费后净盈利**计） |
| 总收益率 | 已实现盈亏 ÷ 初始资金 |

### 14.2.2 资金走势（资金曲线）

- **资金曲线**：每次**平仓/减仓结算后**记录一个点 `(时间, 总资产)`，连成折线
  - 时间 = 该 K 线时间；总资产 = 钱包 + 账户（结算后）
  - 当前持仓期间：显示实时浮盈点位（虚线延伸），方便看持仓中权益
- 曲线附带：
  - **最大回撤**：曲线峰值到谷值的最大跌幅（%）
  - **当前回撤**
- 绘制：复用 canvas（独立小图 or 面板内嵌）

### 14.2.3 盈亏比分析

| 指标 | 定义 |
|---|---|
| 平均盈利 / 平均亏损 | 盈利交易均值 / 亏损交易均值（USDT） |
| 盈亏比（RR） | 平均盈利 ÷ 平均亏损（绝对值） |
| Profit Factor | 总盈利 ÷ 总亏损（>1 为正期望） |
| R 倍数分布 | 每笔盈亏 ÷ 该笔初始风险（开仓到首档止损距离），分布展示 |
| 连盈 / 连亏 | 最长连续盈利 / 亏损笔数 |
| 期望值 | 平均每笔盈亏（USDT） |

- 以数字 + 简单分布（如每笔 R 的柱状/散点）呈现
- 口径：**均按扣费后净盈亏计算**（手续费计入成本）

### 14.2.4 历史买卖点复盘（v1.10 新增）

- 复盘面板提供**"历史买卖点"切换**（显示/隐藏）
- 开启后：所有已平仓持仓的**开仓点（▲）与平仓点（●）标记在 K 线图对应时间/价位**，颜色区分多空与盈亏（盈利绿 / 亏损红）
- 支持**按持仓切换**：查看某笔的完整买卖点序列，或显示全部
- 悬停买卖点显示：时间 / 价格 / 数量 / 触发方式 / 该笔盈亏

### 14.2.5 按维度分组统计（v1.12 新增）

| 维度 | 分组 | 指标 |
|---|---|---|
| 周期 | 15m / 1h / 4h / 1d（按开仓周期） | 笔数 / 胜率 / 总盈亏 / 平均盈亏 |
| 方向 | 多 / 空 | 笔数 / 胜率 / 总盈亏 / 盈亏比 RR |
| 出场方式 | TP / SL / 移动止损 / 手动 / 减仓 | 出场次数 / 盈亏（按各档成交分别计入，与交割单口径一致） |

- 展示：分组表格（每维度一张），可快速看出"在哪个周期/方向/出场方式上赚钱或亏钱"

### 14.2.6 平均持仓时间与交易频率（v1.12 新增）

| 指标 | 定义 |
|---|---|
| 平均持仓时间 | 每笔（完整持仓生命周期）开仓到全平的时间差均值（按 K 线时间） |
| 最长/最短持仓 | 附在平均时间旁 |
| 交易频率 | 总平仓笔数 ÷ 覆盖天数（天 = 首笔开仓到末笔平仓的跨度），即"每天几笔" |

- 展示在概览区（§14.2.1 旁），辅助判断是否过度交易

## 14.3 数据来源

- 已平仓历史（§13.5 历史记录）：开平仓价 / 数量 / **手续费** / 盈亏（扣费后）/ 触发方式 / 时间 / 买卖点
- 资金曲线打点：结算事件（平仓/减仓）后捕获账户权益
- 当前持仓：实时浮盈（最新 K 线价 × 仓位价值 × 方向）

## 14.4 交互

- 交易面板加"统计"入口（按钮/标签页）打开统计面板
- 统计面板可关闭、可重新打开；数据实时刷新
- 资金曲线支持鼠标悬停显示该点时间/资产
- **历史买卖点切换**（显示/隐藏、按笔查看）在统计/复盘面板内
- 统计口径在面板底部说明（如"胜率按完整持仓生命周期计，均已扣手续费"）

## 14.5 验收标准

| # | 用例 | 预期 |
|---|------|------|
| U1 | 平仓一笔盈利 | 已实现盈亏、胜率、资金曲线更新（**扣手续费后**） |
| U2 | 资金曲线 | 结算后打点正确，曲线随交易递增/递减 |
| U3 | 最大回撤 | 计算正确（构造先盈后亏序列验证） |
| U4 | 盈亏比/RR/PF | 按定义计算正确（多笔样本验证，扣费口径） |
| U5 | R 倍数分布 | 每笔 R = 盈亏÷初始风险，展示正确 |
| U6 | 未实现盈亏 | 持仓中显示实时浮盈，平仓后归入已实现 |
| U7 | **手续费** | 开/平仓按名义价值×费率扣费，结算与统计含费（构造样本验证） |
| U8 | **历史买卖点切换** | 复盘开关显示/隐藏买卖点，标记位置与颜色正确 |
| U9 | **交割单导出** | CSV 含全部成交流水（含手续费），Excel 打开中文不乱码 |
| U10 | **分组统计** | 按周期/方向/出场方式分组，数值与口径正确 |
| U11 | **平均持仓时间/频率** | 计算正确（构造样本验证） |
| U12 | **移动止损统计** | 移动止损出场的交易归入对应分组 |
| U13 | 持久化 | 刷新后统计保留 |
| U14 | 回归 | 交易/画线/缩放不受影响 |

## 14.6 非目标（本期不做）

- 不做复杂回测引擎（仅统计实际模拟交易）
- 不做多标的组合分析（单 BTC）
- 不做收益率年化/夏普等高级指标（可后续）

## 14.7 测试策略

1. **单元**：胜率、盈亏比 RR、Profit Factor、最大回撤、R 倍数、**手续费**、**分组统计**、**平均持仓时间/频率**、**移动止损跟踪**计算（构造样本断言）
2. **集成**：模拟平仓序列 → 资金曲线打点正确（含费）；买卖点标记；Trailing SL 推进
3. **回归**：现有 87 项全过

## 14.8 交付物

| 文件 | 内容 |
| --- | --- |
| `index.html` | 统计面板（概览/资金曲线/盈亏比/分组/持仓时间/历史买卖点/交割单导出/移动止损） |
| `tests/regression.test.cjs` | 统计/手续费/分组/Trailing SL 测试 |
| `PRD.md` | 本文档（v1.12） |

## 14.9 风险与对策

| 风险 | 对策 |
|------|------|
| 资金曲线口径不清（含不含浮盈） | 明确：结算后打点用钱包+账户；持仓中虚线显示含浮盈 |
| 统计口径歧义（胜率按笔还是按部分） | 按"完整持仓生命周期"计笔；减仓计入该持仓已实现盈亏 |
| 回归风险 | 独立模块；跑全量 regression |

## 14.10 完成定义

- [ ] 自动化测试全过（含统计/手续费/分组/持仓时间/交割单/Trailing SL 测试）
- [ ] 手动验收 U1–U14、T13–T14（mac 实测）
- [ ] 提交 feature/sim-trade 分支，评审后合入 main

---

# 附：§15 模拟交易测试用例（PRD 配套测试计划）

版本：v1.13 追加 ｜ 日期：2026-08-14 ｜ 状态：待评审

> 配合 §13/§14 使用。自动化为 `tests/regression.test.cjs`（mock 环境驱动），
> 手动项在 mac/win 实机验收。用例编号：S（资金/开仓/结算）、O（挂单/止损）、M（统计/导出）。

## 15.1 资金与手续费

| # | 用例 | 操作 | 预期 |
|---|------|------|------|
| S1 | 初始资金 | 首次进入 | 钱包 10000 USDT，账户 0 |
| S2 | 划转入账户 | 钱包→账户 3000 | 钱包 7000，账户 3000 |
| S3 | 划转回钱包 | 账户→钱包 500 | 双向余额正确 |
| S4 | 超额划转 | 划转 > 可用 | 拒绝/钳制，余额不变 |
| S5 | 手续费计算 | 1000U×5x 开仓 | 名义价值 5000U，开仓费 = 5000×费率 |
| S6 | 费率可配置 | 修改费率 | 后续成交按新费率 |
| S7 | 扣费口径 | 平仓后查看已实现 | = 价格盈亏 − 开仓费 − 平仓费 |

## 15.2 开仓

| # | 用例 | 操作 | 预期 |
|---|------|------|------|
| S8 | 开多 5x | 最新价开多，保证金 500 | 仓位数量 = 500×5÷价；账户扣 500 保证金 |
| S9 | 开空 10x | 最新价开空，保证金 300 | 方向正确；已占用 += 300 |
| S10 | 保证金不足 | 输入 > 账户可用 | 拒绝开仓并提示 |
| S11 | 开仓价来源 | 开仓后仓位线位置 | = 当前视图最新 K 线收盘价 |
| S12 | 图表仓位线 | 开仓后 | 显示成本线 + 方向/杠杆/数量标识 |

## 15.3 加仓 / 减仓

| # | 用例 | 操作 | 预期 |
|---|------|------|------|
| S13 | 加仓 | 持仓中追加保证金 | 数量增加；成本价 = 加权平均 |
| S14 | 减仓 | 持仓中卖出一部分 | 该部分按当前价结算，盈亏入账 |
| S15 | 减仓后挂单 | 减仓后检查挂单 | 挂单比例不变，对应数量按新持仓量 |
| S16 | 加减仓手续费 | 加/减仓 | 按该笔名义价值 × 费率扣费 |

## 15.4 挂单与分批止盈止损

| # | 用例 | 操作 | 预期 |
|---|------|------|------|
| O1 | 拖动生成 TP | 多头线上拖 | 生成 TP 挂单，方向正确 |
| O2 | 拖动生成 SL | 多头线下拖 | 生成 SL 挂单 |
| O3 | 空头方向 | 空头上下拖 | 下=TP、上=SL（与多头相反） |
| O4 | 数量设置 | 设置卖出比例 | 挂单数量 = 持仓量 × 比例 |
| O5 | 多档 TP | 挂 TP1/TP2/TP3 | 各自独立触发 |
| O6 | 多档 SL | 挂 SL1/SL2 | 分批止损生效 |
| O7 | 调整/删除 | 再拖动/删除 | 挂单更新或移除 |

## 15.5 移动止损 Trailing SL

| # | 用例 | 操作 | 预期 |
|---|------|------|------|
| O8 | 多头跟踪 | 价格上行推进 | SL 只上移，跟住最高价−距离 |
| O9 | 空头跟踪 | 价格下行推进 | SL 只下移 |
| O10 | 保本移动 | 盈利过开仓价 | SL 至少移到开仓价 |
| O11 | 回落触发 | 价格回落到移动后 SL | 触发平仓 |

## 15.6 推进触发与结算

| # | 用例 | 操作 | 预期 |
|---|------|------|------|
| O12 | K 线穿越触发 | 推进到穿越挂单价的 K 线 | 挂单成交，提示正确 |
| O13 | 同段多档 | 一根 K 线穿多档 | 按顺序逐档成交 |
| O14 | 分批结算 | TP1 成交 | 只卖该档数量，剩余持有 |
| O15 | 全平关闭 | 最后一档成交 | 持仓关闭，历史记录 |
| O16 | 手动平仓 | 点平仓 | 按当前价全平结算 |
| O17 | 爆仓 | 亏损 ≥ 保证金 | 自动平仓，该笔保证金归零 |

## 15.7 统计

| # | 用例 | 操作 | 预期 |
|---|------|------|------|
| M1 | 胜率 | 构造 3 盈 2 亏 | 胜率 60%（扣费后净盈利计） |
| M2 | 盈亏比 RR | 平均盈 100 / 亏 50 | RR = 2 |
| M3 | Profit Factor | 总盈 300 / 总亏 150 | PF = 2 |
| M4 | R 倍数 | 每笔盈亏÷初始风险 | 分布正确 |
| M5 | 资金曲线 | 平仓序列 | 结算后打点，曲线趋势正确 |
| M6 | 最大回撤 | 先盈后亏序列 | 回撤 = 峰值→谷值 % |
| M7 | 未实现盈亏 | 持仓中 | 显示浮盈（最新价），平仓归入已实现 |

## 15.8 分组 / 持仓时间 / 频率

| # | 用例 | 操作 | 预期 |
|---|------|------|------|
| M8 | 按周期分组 | 15m/1d 各交易 | 各周期笔数/胜率/盈亏正确 |
| M9 | 按方向分组 | 多空各交易 | 多空对比正确 |
| M10 | 按出场方式 | TP/SL/手动混合 | 按各档成交分别计入 |
| M11 | 平均持仓时间 | 构造不同持仓时长 | 均值/最长/最短正确 |
| M12 | 交易频率 | N 笔 / D 天 | 每天 N/D 笔 |

## 15.9 交割单 / 买卖点 / 备注 / 持久化

| # | 用例 | 操作 | 预期 |
|---|------|------|------|
| M13 | 交割单导出 | 点导出 | CSV 含全部成交流水（含手续费），Excel 中文不乱码 |
| M14 | 买卖点切换 | 复盘开关 | 图上开仓▲/平仓● 标记正确，按笔可查 |
| M15 | 交易备注 | 添加/编辑 | 持仓/历史显示，交割单含备注列 |
| M16 | 持久化 | 刷新页面 | 持仓/挂单/余额/统计保留 |
| M17 | 回归 | 跑全量 | 现有 87 项全过 |

## 15.10 完成定义

- [ ] M1–M17 / O1–O17 / S1–S16 全部通过（自动化 + 手动）
- [ ] 现有回归 87 项全过
